{"id": "S00039", "language": "python", "difficulty": "hard", "problem_statement": "A restoration plan for a power grid is modeled as a DAG. Each station can be started only after all of its prerequisites have been started in earlier rounds, except that stations within the same round may be started together only if none depends directly or indirectly on another station in that same round. You are given a subset of target stations that must all become active, and you may also start any prerequisite stations required for them. Compute the minimum number of rounds needed. The objective is the number of synchronous startup rounds under precedence layers for the closure of the target set, not the total number of stations or the longest path in the whole graph unrelated to the targets.", "function_signature": "def min_startup_rounds(n: int, edges: list[tuple[int, int]], targets: list[int]) -> int:", "input_format": "There are n stations labeled 0 to n-1. Each edge (u, v) means u must be started in an earlier round before v can be started. targets lists stations that must end up active.", "output_format": "Return the minimum number of rounds required to activate every target station along with all prerequisites needed for them.", "constraints": ["1 <= n <= 3e5", "0 <= len(edges) <= 4e5", "1 <= len(targets) <= n", "the dependency graph is acyclic"], "solver_prompt": "Solve this problem in python.\n\nA restoration plan for a power grid is modeled as a DAG. Each station can be started only after all of its prerequisites have been started in earlier rounds, except that stations within the same round may be started together only if none depends directly or indirectly on another station in that same round. You are given a subset of target stations that must all become active, and you may also start any prerequisite stations required for them. Compute the minimum number of rounds needed. The objective is the number of synchronous startup rounds under precedence layers for the closure of the target set, not the total number of stations or the longest path in the whole graph unrelated to the targets.\n\nFunction signature:\ndef min_startup_rounds(n: int, edges: list[tuple[int, int]], targets: list[int]) -> int:\n\nInput:\nThere are n stations labeled 0 to n-1. Each edge (u, v) means u must be started in an earlier round before v can be started. targets lists stations that must end up active.\n\nOutput:\nReturn the minimum number of rounds required to activate every target station along with all prerequisites needed for them.\n\nConstraints:\n- 1 <= n <= 3e5\n- 0 <= len(edges) <= 4e5\n- 1 <= len(targets) <= n\n- the dependency graph is acyclic\n\nReturn only code."}
{"id": "S01029", "language": "python", "difficulty": "hard", "problem_statement": "A school stores attendance marks as `(course_id, student_id, day, mark)` where `mark` is `P`, `A`, or `L`. For each course, produce one output row for every student who appears in the global student roster, even if that student has no marks in that course. The row format is `(course_id, student_id, present_days, absent_days, late_days, final_mark)`, where `final_mark` is the mark from the greatest day number for that student in that course, or `'-'` if no mark exists. When multiple records exist for the same `(course_id, student_id, day)`, keep only the last one from the input. Return rows sorted by course ID and then student ID.", "function_signature": "def rollup_attendance(roster: list[str], marks: list[tuple[str, str, int, str]]) -> list[tuple[str, str, int, int, int, str]]:", "input_format": "The first argument is the global student roster as a list of distinct student IDs. The second is a list of 4-tuples `(course_id, student_id, day, mark)` in original arrival order.", "output_format": "Return a list of tuples `(course_id, student_id, present_days, absent_days, late_days, final_mark)` sorted by course ID and student ID.", "constraints": ["1 <= len(roster) <= 200000", "1 <= len(marks) <= 400000", "day is a positive integer", "mark is one of 'P', 'A', 'L'"], "solver_prompt": "Solve this problem in python.\n\nA school stores attendance marks as `(course_id, student_id, day, mark)` where `mark` is `P`, `A`, or `L`. For each course, produce one output row for every student who appears in the global student roster, even if that student has no marks in that course. The row format is `(course_id, student_id, present_days, absent_days, late_days, final_mark)`, where `final_mark` is the mark from the greatest day number for that student in that course, or `'-'` if no mark exists. When multiple records exist for the same `(course_id, student_id, day)`, keep only the last one from the input. Return rows sorted by course ID and then student ID.\n\nFunction signature:\ndef rollup_attendance(roster: list[str], marks: list[tuple[str, str, int, str]]) -> list[tuple[str, str, int, int, int, str]]:\n\nInput:\nThe first argument is the global student roster as a list of distinct student IDs. The second is a list of 4-tuples `(course_id, student_id, day, mark)` in original arrival order.\n\nOutput:\nReturn a list of tuples `(course_id, student_id, present_days, absent_days, late_days, final_mark)` sorted by course ID and student ID.\n\nConstraints:\n- 1 <= len(roster) <= 200000\n- 1 <= len(marks) <= 400000\n- day is a positive integer\n- mark is one of 'P', 'A', 'L'\n\nReturn only code."}
{"id": "S01007", "language": "python", "difficulty": "hard", "problem_statement": "A media tool receives subtitle segments for multiple languages. Each segment has a language code, start millisecond, end millisecond, and text. For each language independently, merge consecutive segments only when the end of one equals the start of the next and the text strings are exactly identical. Segments with empty text must be discarded before any merging. After processing, return a manifest list sorted by language code and then by start time, where each item is a four-field tuple. Overlapping equal-text segments must not be merged unless they are exactly consecutive, and zero-length segments vanish if their text is empty but otherwise remain valid.", "function_signature": "def subtitle_manifest(segments: list[tuple[str, int, int, str]]) -> list[tuple[str, int, int, str]]:", "input_format": "A list of tuples (language, start_ms, end_ms, text). language is a lowercase code string.", "output_format": "Return a sorted list of tuples (language, merged_start_ms, merged_end_ms, text).", "constraints": ["1 <= len(segments) <= 250000", "0 <= start_ms <= end_ms <= 10^12", "Total text length across input <= 10^7"], "solver_prompt": "Solve this problem in python.\n\nA media tool receives subtitle segments for multiple languages. Each segment has a language code, start millisecond, end millisecond, and text. For each language independently, merge consecutive segments only when the end of one equals the start of the next and the text strings are exactly identical. Segments with empty text must be discarded before any merging. After processing, return a manifest list sorted by language code and then by start time, where each item is a four-field tuple. Overlapping equal-text segments must not be merged unless they are exactly consecutive, and zero-length segments vanish if their text is empty but otherwise remain valid.\n\nFunction signature:\ndef subtitle_manifest(segments: list[tuple[str, int, int, str]]) -> list[tuple[str, int, int, str]]:\n\nInput:\nA list of tuples (language, start_ms, end_ms, text). language is a lowercase code string.\n\nOutput:\nReturn a sorted list of tuples (language, merged_start_ms, merged_end_ms, text).\n\nConstraints:\n- 1 <= len(segments) <= 250000\n- 0 <= start_ms <= end_ms <= 10^12\n- Total text length across input <= 10^7\n\nReturn only code."}
{"id": "S70009", "language": "cpp", "difficulty": "hard", "problem_statement": "A monitoring hub tracks services and watcher subscriptions. Commands register a service, retire a service, subscribe a watcher to a service version, migrate a service to a new version id, emit a health pulse to a version, and ask how many pulses each watcher has successfully received. A pulse reaches a watcher only if the subscribed version is the current live representative of that service lineage at emit time. When a service migrates, the old version id becomes retired and all existing subscriptions to the retired live version automatically follow the migration; subscriptions to already retired versions do not revive. Implement the processor and return watcher counts at each report command.", "function_signature": "std::vector<long long> monitorHub(const std::vector<std::vector<long long>>& cmds);", "input_format": "Commands are vectors with type code first: [1, service_version], [2, service_version], [3, watcher, service_version], [4, old_version, new_version], [5, service_version], [6, watcher].", "output_format": "Return one integer per type-6 report command representing that watcher's cumulative received pulse count.", "constraints": ["1 <= cmds.size() <= 220000", "Watcher ids and version ids fit in 64-bit signed integers", "A watcher may subscribe many times; only the latest active subscription for that watcher matters"], "solver_prompt": "Solve this problem in cpp.\n\nA monitoring hub tracks services and watcher subscriptions. Commands register a service, retire a service, subscribe a watcher to a service version, migrate a service to a new version id, emit a health pulse to a version, and ask how many pulses each watcher has successfully received. A pulse reaches a watcher only if the subscribed version is the current live representative of that service lineage at emit time. When a service migrates, the old version id becomes retired and all existing subscriptions to the retired live version automatically follow the migration; subscriptions to already retired versions do not revive. Implement the processor and return watcher counts at each report command.\n\nFunction signature:\nstd::vector<long long> monitorHub(const std::vector<std::vector<long long>>& cmds);\n\nInput:\nCommands are vectors with type code first: [1, service_version], [2, service_version], [3, watcher, service_version], [4, old_version, new_version], [5, service_version], [6, watcher].\n\nOutput:\nReturn one integer per type-6 report command representing that watcher's cumulative received pulse count.\n\nConstraints:\n- 1 <= cmds.size() <= 220000\n- Watcher ids and version ids fit in 64-bit signed integers\n- A watcher may subscribe many times; only the latest active subscription for that watcher matters\n\nReturn only code."}
{"id": "S01071", "language": "python", "difficulty": "hard", "problem_statement": "A transit agency records bay assignment changes for buses throughout a day. Each event names a station, a bay token, a route code, a minute of day, and an action of ARRIVE or DEPART. For every station listed in the input roster, produce a final occupancy board after replaying all events in chronological order, using input order to break ties at the same minute. A bus occupies a bay after its ARRIVE until a matching DEPART for the same station, bay, and route removes it. Output one line per station-bay pair that is occupied at the end, sorted by station, then bays using numeric comparison for purely numeric bay tokens and lexicographic comparison otherwise. If a station ends with no occupied bays, output exactly one placeholder line for that station with bay \"-\" and route \"EMPTY\".", "function_signature": "def build_bay_board(stations: list[str], events: list[dict]) -> list[str]:", "input_format": "stations is a list of distinct station names. events is a list of dictionaries with keys: station (str), bay (str), route (str), minute (int), action (str).", "output_format": "Return a list of strings in the form \"station|bay|route\". Stations with no occupied bays must contribute exactly one line in the form \"station|-|EMPTY\".", "constraints": ["1 <= len(stations) <= 2 * 10^4", "0 <= len(events) <= 3 * 10^5", "0 <= minute <= 1439", "Station names in events always appear in stations"], "solver_prompt": "Solve this problem in python.\n\nA transit agency records bay assignment changes for buses throughout a day. Each event names a station, a bay token, a route code, a minute of day, and an action of ARRIVE or DEPART. For every station listed in the input roster, produce a final occupancy board after replaying all events in chronological order, using input order to break ties at the same minute. A bus occupies a bay after its ARRIVE until a matching DEPART for the same station, bay, and route removes it. Output one line per station-bay pair that is occupied at the end, sorted by station, then bays using numeric comparison for purely numeric bay tokens and lexicographic comparison otherwise. If a station ends with no occupied bays, output exactly one placeholder line for that station with bay \"-\" and route \"EMPTY\".\n\nFunction signature:\ndef build_bay_board(stations: list[str], events: list[dict]) -> list[str]:\n\nInput:\nstations is a list of distinct station names. events is a list of dictionaries with keys: station (str), bay (str), route (str), minute (int), action (str).\n\nOutput:\nReturn a list of strings in the form \"station|bay|route\". Stations with no occupied bays must contribute exactly one line in the form \"station|-|EMPTY\".\n\nConstraints:\n- 1 <= len(stations) <= 2 * 10^4\n- 0 <= len(events) <= 3 * 10^5\n- 0 <= minute <= 1439\n- Station names in events always appear in stations\n\nReturn only code."}
{"id": "S01063", "language": "python", "difficulty": "hard", "problem_statement": "A bioinformatics service tracks badges attached to inclusive genomic segments. Each record gives a chromosome, start, end, badge, priority, and revision. For any covered base, the visible badge is chosen by highest priority, then highest revision, then later input position. Produce a canonical export of maximal runs with the same visible badge for each chromosome. Gaps must not be filled, touching runs with the same badge should merge only when coverage is continuous, and chromosomes seen in input but having no surviving coverage after filtering invalid records must still appear as one empty export line with a fixed badge token and a literal coordinate pair.", "function_signature": "def export_badge_runs(records: list[dict]) -> list[str]:", "input_format": "records is a list of dictionaries with keys: chrom (str), start (int), end (int), badge (str), priority (int), revision (int). Records with start > end are invalid and ignored.", "output_format": "Return lines of the form \"<chrom>:<start>-<end>=<badge>\" using inclusive coordinates. For a chromosome with no surviving coverage, return exactly \"<chrom>:0--1=EMPTY\".", "constraints": ["1 <= len(records) <= 120000", "0 <= start, end <= 10^9", "1 <= len(chrom), len(badge) <= 20"], "solver_prompt": "Solve this problem in python.\n\nA bioinformatics service tracks badges attached to inclusive genomic segments. Each record gives a chromosome, start, end, badge, priority, and revision. For any covered base, the visible badge is chosen by highest priority, then highest revision, then later input position. Produce a canonical export of maximal runs with the same visible badge for each chromosome. Gaps must not be filled, touching runs with the same badge should merge only when coverage is continuous, and chromosomes seen in input but having no surviving coverage after filtering invalid records must still appear as one empty export line with a fixed badge token and a literal coordinate pair.\n\nFunction signature:\ndef export_badge_runs(records: list[dict]) -> list[str]:\n\nInput:\nrecords is a list of dictionaries with keys: chrom (str), start (int), end (int), badge (str), priority (int), revision (int). Records with start > end are invalid and ignored.\n\nOutput:\nReturn lines of the form \"<chrom>:<start>-<end>=<badge>\" using inclusive coordinates. For a chromosome with no surviving coverage, return exactly \"<chrom>:0--1=EMPTY\".\n\nConstraints:\n- 1 <= len(records) <= 120000\n- 0 <= start, end <= 10^9\n- 1 <= len(chrom), len(badge) <= 20\n\nReturn only code."}
{"id": "S01058", "language": "python", "difficulty": "hard", "problem_statement": "A telemetry archive stores fragments of buoy message frames. Each fragment has buoy id, frame id, part index starting at 0, total parts, payload text, and a validity flag. For a frame to be emitted, all part indices from 0 to total_parts-1 must be present among valid fragments for that same buoy and frame, and all surviving fragments for the frame must agree on total_parts after keeping only the last valid fragment per part index in input order. When a frame is complete, concatenate payload texts in ascending part index. Output one line per emitted frame in ascending buoy id, then ascending frame id, using the exact format buoy|frame|count|message where count is total_parts as decimal text. For every buoy seen in input that has no emitted frames, output buoy|*|0|NO_COMPLETE_FRAME.", "function_signature": "def canonicalize_buoy_frames(fragments: list[dict]) -> list[str]:", "input_format": "fragments is a list of dictionaries with keys: buoy (str), frame (str), part (int), total (int), payload (str), valid (bool). Only valid fragments participate, and later valid fragments overwrite earlier valid fragments at the same part index.", "output_format": "Return a list of strings in exact buoy|frame|count|message form, with a single placeholder row for buoys lacking any complete frame.", "constraints": ["1 <= len(fragments) <= 190000", "0 <= part < total <= 10000", "payload strings do not contain '|'", "buoy and frame are non-empty strings"], "solver_prompt": "Solve this problem in python.\n\nA telemetry archive stores fragments of buoy message frames. Each fragment has buoy id, frame id, part index starting at 0, total parts, payload text, and a validity flag. For a frame to be emitted, all part indices from 0 to total_parts-1 must be present among valid fragments for that same buoy and frame, and all surviving fragments for the frame must agree on total_parts after keeping only the last valid fragment per part index in input order. When a frame is complete, concatenate payload texts in ascending part index. Output one line per emitted frame in ascending buoy id, then ascending frame id, using the exact format buoy|frame|count|message where count is total_parts as decimal text. For every buoy seen in input that has no emitted frames, output buoy|*|0|NO_COMPLETE_FRAME.\n\nFunction signature:\ndef canonicalize_buoy_frames(fragments: list[dict]) -> list[str]:\n\nInput:\nfragments is a list of dictionaries with keys: buoy (str), frame (str), part (int), total (int), payload (str), valid (bool). Only valid fragments participate, and later valid fragments overwrite earlier valid fragments at the same part index.\n\nOutput:\nReturn a list of strings in exact buoy|frame|count|message form, with a single placeholder row for buoys lacking any complete frame.\n\nConstraints:\n- 1 <= len(fragments) <= 190000\n- 0 <= part < total <= 10000\n- payload strings do not contain '|'\n- buoy and frame are non-empty strings\n\nReturn only code."}
{"id": "S01036", "language": "python", "difficulty": "hard", "problem_statement": "A fulfillment center renames bins through operations `(aisle, old_label, new_label)`. Within each aisle, labels form a directed renaming chain over time, and later operations may target labels produced by earlier ones. After processing all operations in input order, determine the surviving labels and the full history of labels that collapsed into each survivor. Return manifest rows sorted by aisle ascending and then survivor label ascending. Each row must be formatted as `aisle=>survivor<=k:[x1>x2>...>survivor]`, where the bracketed chain lists the distinct labels in transformation order and `k` is the number of labels in that chain. An aisle with several unrelated chains yields several rows.", "function_signature": "def relabel_manifest(ops: list[tuple[str, str, str]]) -> list[str]:", "input_format": "A list of tuples `(aisle, old_label, new_label)` processed in input order. Each operation acts only within one aisle. Labels are strings without the characters `[`, `]`, `<`, `>`, `:`, or `=`.", "output_format": "A sorted list of manifest strings in the exact form `aisle=>survivor<=k:[chain]`.", "constraints": ["1 <= len(ops) <= 300000", "Labels are non-empty strings", "Within an aisle, the renaming graph is acyclic under processing order", "An old label is renamed at most once after it becomes current"], "solver_prompt": "Solve this problem in python.\n\nA fulfillment center renames bins through operations `(aisle, old_label, new_label)`. Within each aisle, labels form a directed renaming chain over time, and later operations may target labels produced by earlier ones. After processing all operations in input order, determine the surviving labels and the full history of labels that collapsed into each survivor. Return manifest rows sorted by aisle ascending and then survivor label ascending. Each row must be formatted as `aisle=>survivor<=k:[x1>x2>...>survivor]`, where the bracketed chain lists the distinct labels in transformation order and `k` is the number of labels in that chain. An aisle with several unrelated chains yields several rows.\n\nFunction signature:\ndef relabel_manifest(ops: list[tuple[str, str, str]]) -> list[str]:\n\nInput:\nA list of tuples `(aisle, old_label, new_label)` processed in input order. Each operation acts only within one aisle. Labels are strings without the characters `[`, `]`, `<`, `>`, `:`, or `=`.\n\nOutput:\nA sorted list of manifest strings in the exact form `aisle=>survivor<=k:[chain]`.\n\nConstraints:\n- 1 <= len(ops) <= 300000\n- Labels are non-empty strings\n- Within an aisle, the renaming graph is acyclic under processing order\n- An old label is renamed at most once after it becomes current\n\nReturn only code."}
{"id": "S70008", "language": "cpp", "difficulty": "hard", "problem_statement": "An e-commerce platform replays an append-only event log to reconstruct carts. Events create carts, add items, reserve a checkout snapshot, submit an order from a snapshot, abandon a cart, restore a cart from an earlier snapshot under a new cart id, and query the current total quantity in a cart. A snapshot captures cart contents at the moment it is reserved; once submitted, that snapshot cannot be submitted again. Restored carts are independent live carts even if the original cart is later abandoned or modified. Implement replay so that every query returns the visible total quantity of the addressed live cart or 0 if that cart is not active.", "function_signature": "std::vector<long long> replayCartEvents(const std::vector<std::vector<long long>>& log);", "input_format": "Each log entry begins with a type code: [1, cart], [2, cart, item, delta], [3, cart, snap], [4, snap], [5, cart], [6, snap, new_cart], [7, cart]. Item ids are arbitrary and delta is positive.", "output_format": "Return the total quantity for each type-7 query, in order.", "constraints": ["1 <= log.size() <= 120000", "Cart, item, and snapshot ids are 64-bit integers", "The sum of distinct item updates across all carts does not exceed 300000"], "solver_prompt": "Solve this problem in cpp.\n\nAn e-commerce platform replays an append-only event log to reconstruct carts. Events create carts, add items, reserve a checkout snapshot, submit an order from a snapshot, abandon a cart, restore a cart from an earlier snapshot under a new cart id, and query the current total quantity in a cart. A snapshot captures cart contents at the moment it is reserved; once submitted, that snapshot cannot be submitted again. Restored carts are independent live carts even if the original cart is later abandoned or modified. Implement replay so that every query returns the visible total quantity of the addressed live cart or 0 if that cart is not active.\n\nFunction signature:\nstd::vector<long long> replayCartEvents(const std::vector<std::vector<long long>>& log);\n\nInput:\nEach log entry begins with a type code: [1, cart], [2, cart, item, delta], [3, cart, snap], [4, snap], [5, cart], [6, snap, new_cart], [7, cart]. Item ids are arbitrary and delta is positive.\n\nOutput:\nReturn the total quantity for each type-7 query, in order.\n\nConstraints:\n- 1 <= log.size() <= 120000\n- Cart, item, and snapshot ids are 64-bit integers\n- The sum of distinct item updates across all carts does not exceed 300000\n\nReturn only code."}
{"id": "S01027", "language": "python", "difficulty": "hard", "problem_statement": "A document service receives requested output names as a list of raw invoice labels. For each label, generate a unique file name using the exact rules: trim leading and trailing spaces, replace every maximal run of internal spaces with a single underscore, preserve all other characters, and append `.pdf`. If the resulting base name has not appeared before, use it directly. Otherwise append `(k)` immediately before `.pdf`, where `k` is the smallest positive integer that makes the full name unique among all previously emitted names. Matching is case-sensitive. Return the generated names in input order.", "function_signature": "def plan_invoice_filenames(labels: list[str]) -> list[str]:", "input_format": "A list of raw label strings. Labels may repeat, differ only by spacing, or become equal after normalization.", "output_format": "Return a list of unique output file names, one per input label, in the same order as the input.", "constraints": ["1 <= len(labels) <= 100000", "1 <= len(label) <= 200", "labels are Unicode strings", "the answer must preserve input order exactly"], "solver_prompt": "Solve this problem in python.\n\nA document service receives requested output names as a list of raw invoice labels. For each label, generate a unique file name using the exact rules: trim leading and trailing spaces, replace every maximal run of internal spaces with a single underscore, preserve all other characters, and append `.pdf`. If the resulting base name has not appeared before, use it directly. Otherwise append `(k)` immediately before `.pdf`, where `k` is the smallest positive integer that makes the full name unique among all previously emitted names. Matching is case-sensitive. Return the generated names in input order.\n\nFunction signature:\ndef plan_invoice_filenames(labels: list[str]) -> list[str]:\n\nInput:\nA list of raw label strings. Labels may repeat, differ only by spacing, or become equal after normalization.\n\nOutput:\nReturn a list of unique output file names, one per input label, in the same order as the input.\n\nConstraints:\n- 1 <= len(labels) <= 100000\n- 1 <= len(label) <= 200\n- labels are Unicode strings\n- the answer must preserve input order exactly\n\nReturn only code."}
{"id": "S60002", "language": "python", "difficulty": "hard", "problem_statement": "A compliance team exports audit rows as one giant CSV document. Fields may contain commas, escaped quotes, and embedded line breaks inside quoted cells. Each row contains an account id, event type, amount, and a free-form note. Compute, for every account id, the count of rows, the sum of all amounts as integers, and the lexicographically smallest note among rows whose event type is 'FLAG'. Rows with missing required columns are invalid and must be skipped. Return the per-account summary sorted by account id.", "function_signature": "def reconcile_audit_csv(csv_text: str) -> list[dict]:", "input_format": "A single string containing a header row followed by zero or more CSV rows. Required columns are 'account_id', 'event_type', 'amount', and 'note'. Amount values are base-10 integers that may be negative.", "output_format": "Return a list of dictionaries with keys: 'account_id', 'row_count', 'amount_sum', and 'min_flag_note'. If an account has no FLAG rows, 'min_flag_note' must be None. The list must be sorted by 'account_id'.", "constraints": ["0 <= len(csv_text) <= 5e7", "Up to 1e6 logical CSV rows", "Column order in the file may vary", "Quoted fields may span multiple physical lines"], "solver_prompt": "Solve this problem in python.\n\nA compliance team exports audit rows as one giant CSV document. Fields may contain commas, escaped quotes, and embedded line breaks inside quoted cells. Each row contains an account id, event type, amount, and a free-form note. Compute, for every account id, the count of rows, the sum of all amounts as integers, and the lexicographically smallest note among rows whose event type is 'FLAG'. Rows with missing required columns are invalid and must be skipped. Return the per-account summary sorted by account id.\n\nFunction signature:\ndef reconcile_audit_csv(csv_text: str) -> list[dict]:\n\nInput:\nA single string containing a header row followed by zero or more CSV rows. Required columns are 'account_id', 'event_type', 'amount', and 'note'. Amount values are base-10 integers that may be negative.\n\nOutput:\nReturn a list of dictionaries with keys: 'account_id', 'row_count', 'amount_sum', and 'min_flag_note'. If an account has no FLAG rows, 'min_flag_note' must be None. The list must be sorted by 'account_id'.\n\nConstraints:\n- 0 <= len(csv_text) <= 5e7\n- Up to 1e6 logical CSV rows\n- Column order in the file may vary\n- Quoted fields may span multiple physical lines\n\nReturn only code."}
{"id": "S00015", "language": "python", "difficulty": "hard", "problem_statement": "A row of servers has nonnegative heat outputs. You must split the row into exactly m nonempty contiguous zones. The cost of a zoning is the maximum zone heat sum. Return the minimum possible cost and one valid list of cut indices that achieves it. A solution using fewer than m zones is not valid, even if it produces the same or lower maximum zone sum.", "function_signature": "def optimal_thermal_zones(loads: list[int], m: int) -> tuple[int, list[int]]:", "input_format": "loads is the ordered list of server heat outputs. m is the exact number of contiguous zones required.", "output_format": "Return a pair (best_cost, cuts) where cuts contains the ending indices of the first m-1 zones in increasing order.", "constraints": ["1 <= len(loads) <= 2 * 10^5", "0 <= loads[i] <= 10^9", "1 <= m <= len(loads)", "The optimal cost fits in 64-bit signed integer"], "solver_prompt": "Solve this problem in python.\n\nA row of servers has nonnegative heat outputs. You must split the row into exactly m nonempty contiguous zones. The cost of a zoning is the maximum zone heat sum. Return the minimum possible cost and one valid list of cut indices that achieves it. A solution using fewer than m zones is not valid, even if it produces the same or lower maximum zone sum.\n\nFunction signature:\ndef optimal_thermal_zones(loads: list[int], m: int) -> tuple[int, list[int]]:\n\nInput:\nloads is the ordered list of server heat outputs. m is the exact number of contiguous zones required.\n\nOutput:\nReturn a pair (best_cost, cuts) where cuts contains the ending indices of the first m-1 zones in increasing order.\n\nConstraints:\n- 1 <= len(loads) <= 2 * 10^5\n- 0 <= loads[i] <= 10^9\n- 1 <= m <= len(loads)\n- The optimal cost fits in 64-bit signed integer\n\nReturn only code."}
{"id": "S01023", "language": "python", "difficulty": "hard", "problem_statement": "An irrigation controller emits state changes for valves on a 24-hour circular clock as `(valve_id, minute, state)`, where `state` is `OPEN` or `CLOSED`. Minute values are in `[0, 1439]`, and day start is minute `0`. Treat the final state at minute `1439` as continuing through the wrap to the next day until the first state change of that valve. Produce compressed open intervals for one canonical day as closed-open ranges `[start, end)` with `0 <= start < end <= 1440`. If a valve is open across midnight, split the result into two intervals, one ending at `1440` and one starting at `0`. Output intervals sorted by valve ID, then start minute.", "function_signature": "def compress_valve_cycles(events: list[tuple[str, int, str]]) -> list[tuple[str, int, int]]:", "input_format": "A list of 3-tuples `(valve_id, minute, state)`. Events may be unsorted and multiple changes may exist for the same minute and valve.", "output_format": "Return a list of tuples `(valve_id, start_minute, end_minute)` representing canonical open intervals, sorted by valve ID and start minute.", "constraints": ["1 <= len(events) <= 1000000", "0 <= minute <= 1439", "state is either 'OPEN' or 'CLOSED'", "valve_id is a non-empty ASCII string"], "solver_prompt": "Solve this problem in python.\n\nAn irrigation controller emits state changes for valves on a 24-hour circular clock as `(valve_id, minute, state)`, where `state` is `OPEN` or `CLOSED`. Minute values are in `[0, 1439]`, and day start is minute `0`. Treat the final state at minute `1439` as continuing through the wrap to the next day until the first state change of that valve. Produce compressed open intervals for one canonical day as closed-open ranges `[start, end)` with `0 <= start < end <= 1440`. If a valve is open across midnight, split the result into two intervals, one ending at `1440` and one starting at `0`. Output intervals sorted by valve ID, then start minute.\n\nFunction signature:\ndef compress_valve_cycles(events: list[tuple[str, int, str]]) -> list[tuple[str, int, int]]:\n\nInput:\nA list of 3-tuples `(valve_id, minute, state)`. Events may be unsorted and multiple changes may exist for the same minute and valve.\n\nOutput:\nReturn a list of tuples `(valve_id, start_minute, end_minute)` representing canonical open intervals, sorted by valve ID and start minute.\n\nConstraints:\n- 1 <= len(events) <= 1000000\n- 0 <= minute <= 1439\n- state is either 'OPEN' or 'CLOSED'\n- valve_id is a non-empty ASCII string\n\nReturn only code."}
{"id": "S00027", "language": "python", "difficulty": "hard", "problem_statement": "A league table is computed from match results. Teams receive 3 points for a win, 1 for a draw, and 0 for a loss. Rankings are sorted by total points descending, then goal difference descending, then goals scored descending, then team name ascending. Given all matches in arbitrary order, return the final ordered table of team names. Every match contributes simultaneously to both participating teams' statistics, and all tiebreakers must be based on season totals rather than head-to-head subsets.", "function_signature": "def rank_league_table(matches: list[tuple[str, str, int, int]]) -> list[str]:", "input_format": "Each match tuple is (home_team, away_team, home_goals, away_goals). Team names are case-sensitive strings. Every team appearing in any match must appear in the final ranking.", "output_format": "Return team names in final ranking order from best to worst.", "constraints": ["1 <= len(matches) <= 200000", "1 <= number of distinct teams <= 100000", "0 <= home_goals, away_goals <= 1000"], "solver_prompt": "Solve this problem in python.\n\nA league table is computed from match results. Teams receive 3 points for a win, 1 for a draw, and 0 for a loss. Rankings are sorted by total points descending, then goal difference descending, then goals scored descending, then team name ascending. Given all matches in arbitrary order, return the final ordered table of team names. Every match contributes simultaneously to both participating teams' statistics, and all tiebreakers must be based on season totals rather than head-to-head subsets.\n\nFunction signature:\ndef rank_league_table(matches: list[tuple[str, str, int, int]]) -> list[str]:\n\nInput:\nEach match tuple is (home_team, away_team, home_goals, away_goals). Team names are case-sensitive strings. Every team appearing in any match must appear in the final ranking.\n\nOutput:\nReturn team names in final ranking order from best to worst.\n\nConstraints:\n- 1 <= len(matches) <= 200000\n- 1 <= number of distinct teams <= 100000\n- 0 <= home_goals, away_goals <= 1000\n\nReturn only code."}
{"id": "S00032", "language": "cpp", "difficulty": "hard", "problem_statement": "A museum records temperature readings from many rooms across a circular weekly schedule. Each reading has a wing id, a room id, a minute-of-week timestamp, and a temperature. For every room, compress consecutive readings whose rounded temperature is unchanged, then report all maximal time ranges where a wing has at least K rooms simultaneously above a threshold T. The object receives readings incrementally, and the final report must be emitted in chronological order across all wings after ingestion finishes.", "function_signature": "class ClimateSummary { public: explicit ClimateSummary(int threshold, int requiredRooms); void ingest(int wingId, int roomId, int minuteOfWeek, int temperature); vector<array<int,4>> finalize() const; };", "input_format": "The constructor receives threshold T and requiredRooms K. Each ingest call provides one reading. finalize returns records of the form {wingId, startMinute, endMinute, countAboveThreshold}. Minutes are in [0, 10079].", "output_format": "Return a vector of arrays sorted by wingId, then by startMinute, containing every maximal interval where at least K rooms in the wing are above T, together with the peak simultaneous count over that interval.", "constraints": ["1 <= total readings <= 4e5", "1 <= number of wings + rooms <= 1e5", "0 <= minuteOfWeek < 10080", "-50 <= temperature <= 150"], "solver_prompt": "Solve this problem in cpp.\n\nA museum records temperature readings from many rooms across a circular weekly schedule. Each reading has a wing id, a room id, a minute-of-week timestamp, and a temperature. For every room, compress consecutive readings whose rounded temperature is unchanged, then report all maximal time ranges where a wing has at least K rooms simultaneously above a threshold T. The object receives readings incrementally, and the final report must be emitted in chronological order across all wings after ingestion finishes.\n\nFunction signature:\nclass ClimateSummary { public: explicit ClimateSummary(int threshold, int requiredRooms); void ingest(int wingId, int roomId, int minuteOfWeek, int temperature); vector<array<int,4>> finalize() const; };\n\nInput:\nThe constructor receives threshold T and requiredRooms K. Each ingest call provides one reading. finalize returns records of the form {wingId, startMinute, endMinute, countAboveThreshold}. Minutes are in [0, 10079].\n\nOutput:\nReturn a vector of arrays sorted by wingId, then by startMinute, containing every maximal interval where at least K rooms in the wing are above T, together with the peak simultaneous count over that interval.\n\nConstraints:\n- 1 <= total readings <= 4e5\n- 1 <= number of wings + rooms <= 1e5\n- 0 <= minuteOfWeek < 10080\n- -50 <= temperature <= 150\n\nReturn only code."}
{"id": "S01009", "language": "python", "difficulty": "hard", "problem_statement": "An environmental sensor mesh is described by a set of active lattice cells on an unbounded 2D plane. Two cells belong to the same island if they share a side. Label all islands by increasing smallest cell in lexicographic order (row first, then column), and for each island report its label, area, perimeter, and bounding box. The final output must be a list of dictionaries in label order. Coordinates may be negative, active cells are unique, and the perimeter counts edges adjacent to empty space. The bounding box fields must use keys 'min_r', 'max_r', 'min_c', and 'max_c' exactly.", "function_signature": "def label_islands(cells: list[tuple[int, int]]) -> list[dict]:", "input_format": "A list of unique coordinate pairs (row, col) representing active cells.", "output_format": "Return a list of dictionaries, each with keys 'label', 'area', 'perimeter', 'min_r', 'max_r', 'min_c', and 'max_c'.", "constraints": ["1 <= len(cells) <= 200000", "-10^9 <= row, col <= 10^9"], "solver_prompt": "Solve this problem in python.\n\nAn environmental sensor mesh is described by a set of active lattice cells on an unbounded 2D plane. Two cells belong to the same island if they share a side. Label all islands by increasing smallest cell in lexicographic order (row first, then column), and for each island report its label, area, perimeter, and bounding box. The final output must be a list of dictionaries in label order. Coordinates may be negative, active cells are unique, and the perimeter counts edges adjacent to empty space. The bounding box fields must use keys 'min_r', 'max_r', 'min_c', and 'max_c' exactly.\n\nFunction signature:\ndef label_islands(cells: list[tuple[int, int]]) -> list[dict]:\n\nInput:\nA list of unique coordinate pairs (row, col) representing active cells.\n\nOutput:\nReturn a list of dictionaries, each with keys 'label', 'area', 'perimeter', 'min_r', 'max_r', 'min_c', and 'max_c'.\n\nConstraints:\n- 1 <= len(cells) <= 200000\n- -10^9 <= row, col <= 10^9\n\nReturn only code."}
{"id": "S01008", "language": "python", "difficulty": "hard", "problem_statement": "A sports archive stores results from many race heats. Each heat provides an ordered list of athlete IDs from first to last, but some heats contain duplicate IDs due to recording faults. For a heat with duplicates, keep only the first occurrence of each athlete and drop later repeats in that heat. Compute season points using the placement table [10, 6, 4, 3, 2, 1] for the first six retained places of every heat. Then return the podium table as a list of strings for athletes with nonzero points, sorted by descending points, then by number of heat wins, then by athlete ID ascending. Each string must contain the athlete ID, points, wins, and heats_scored separated by commas with no spaces.", "function_signature": "def podium_table(heats: list[list[int]]) -> list[str]:", "input_format": "A list of heats, where each heat is a list of integer athlete IDs in recorded finish order.", "output_format": "Return strings formatted exactly as 'athlete_id,points,wins,heats_scored'.", "constraints": ["1 <= len(heats) <= 100000", "Total number of recorded finish positions across all heats <= 600000", "Athlete IDs fit in signed 32-bit integers"], "solver_prompt": "Solve this problem in python.\n\nA sports archive stores results from many race heats. Each heat provides an ordered list of athlete IDs from first to last, but some heats contain duplicate IDs due to recording faults. For a heat with duplicates, keep only the first occurrence of each athlete and drop later repeats in that heat. Compute season points using the placement table [10, 6, 4, 3, 2, 1] for the first six retained places of every heat. Then return the podium table as a list of strings for athletes with nonzero points, sorted by descending points, then by number of heat wins, then by athlete ID ascending. Each string must contain the athlete ID, points, wins, and heats_scored separated by commas with no spaces.\n\nFunction signature:\ndef podium_table(heats: list[list[int]]) -> list[str]:\n\nInput:\nA list of heats, where each heat is a list of integer athlete IDs in recorded finish order.\n\nOutput:\nReturn strings formatted exactly as 'athlete_id,points,wins,heats_scored'.\n\nConstraints:\n- 1 <= len(heats) <= 100000\n- Total number of recorded finish positions across all heats <= 600000\n- Athlete IDs fit in signed 32-bit integers\n\nReturn only code."}
{"id": "S01024", "language": "python", "difficulty": "hard", "problem_statement": "A tournament system logs events for each game as `(game_id, player, phase, seconds, kind)`, where `phase` is one of `OPENING`, `MIDDLEGAME`, `ENDGAME` and `kind` is `MOVE`, `FLAG`, or `PENALTY`. For each game, compute the losing player and the official reason. A `FLAG` loses immediately unless both players flag in the same phase and at the same second, in which case the loser is the lexicographically larger player ID. A `PENALTY` loses immediately and overrides any later `FLAG`. If both players receive penalties at the same second in the same phase, the loser is the player with more prior `MOVE` events in that game; if still tied, lexicographically larger player ID loses. Return `(game_id, loser, reason)` sorted by game ID.", "function_signature": "def build_penalty_report(events: list[tuple[str, str, str, int, str]]) -> list[tuple[str, str, str]]:", "input_format": "A list of 5-tuples `(game_id, player, phase, seconds, kind)`. Events are not guaranteed to be sorted.", "output_format": "Return a list of tuples `(game_id, loser_player_id, reason)` where reason is exactly `FLAG` or `PENALTY`, sorted by game ID ascending.", "constraints": ["1 <= len(events) <= 80000", "seconds is a non-negative integer", "kind is one of 'MOVE', 'FLAG', 'PENALTY'", "phase is one of 'OPENING', 'MIDDLEGAME', 'ENDGAME'"], "solver_prompt": "Solve this problem in python.\n\nA tournament system logs events for each game as `(game_id, player, phase, seconds, kind)`, where `phase` is one of `OPENING`, `MIDDLEGAME`, `ENDGAME` and `kind` is `MOVE`, `FLAG`, or `PENALTY`. For each game, compute the losing player and the official reason. A `FLAG` loses immediately unless both players flag in the same phase and at the same second, in which case the loser is the lexicographically larger player ID. A `PENALTY` loses immediately and overrides any later `FLAG`. If both players receive penalties at the same second in the same phase, the loser is the player with more prior `MOVE` events in that game; if still tied, lexicographically larger player ID loses. Return `(game_id, loser, reason)` sorted by game ID.\n\nFunction signature:\ndef build_penalty_report(events: list[tuple[str, str, str, int, str]]) -> list[tuple[str, str, str]]:\n\nInput:\nA list of 5-tuples `(game_id, player, phase, seconds, kind)`. Events are not guaranteed to be sorted.\n\nOutput:\nReturn a list of tuples `(game_id, loser_player_id, reason)` where reason is exactly `FLAG` or `PENALTY`, sorted by game ID ascending.\n\nConstraints:\n- 1 <= len(events) <= 80000\n- seconds is a non-negative integer\n- kind is one of 'MOVE', 'FLAG', 'PENALTY'\n- phase is one of 'OPENING', 'MIDDLEGAME', 'ENDGAME'\n\nReturn only code."}
{"id": "S01056", "language": "python", "difficulty": "hard", "problem_statement": "A streaming service stores content nodes in a rooted catalog tree. Each node record gives content id, parent id or null for a root, title, local rating token, and availability token LIVE or REMOVED. The input may list nodes in any order, and all ids are unique. A node with availability REMOVED is excluded entirely along with none of its descendants; descendants remain if their own records are LIVE and connected through existing parents, otherwise they become new roots in the export forest. For every live root of the resulting forest, output one bundle line per node using preorder traversal among children sorted by title ascending then id ascending. Each line must have the exact form root_id::path::effective_rating where path is titles joined by '/', and effective_rating is the nearest non-UNKNOWN local rating on the path from the exported root to the node, or UNRATED if none exists. If no live nodes remain, return ['EMPTY::/::UNRATED'].", "function_signature": "def build_rating_bundles(nodes: list[dict]) -> list[str]:", "input_format": "nodes is a list of dictionaries with keys: id (str), parent (str | None), title (str), rating (str), availability (str). rating may be UNKNOWN or another opaque token string.", "output_format": "Return a list of strings in exact root_id::path::effective_rating form, or the single placeholder string EMPTY::/::UNRATED when applicable.", "constraints": ["1 <= len(nodes) <= 140000", "All ids are unique", "availability is either LIVE or REMOVED", "title strings do not contain '/' or '::'"], "solver_prompt": "Solve this problem in python.\n\nA streaming service stores content nodes in a rooted catalog tree. Each node record gives content id, parent id or null for a root, title, local rating token, and availability token LIVE or REMOVED. The input may list nodes in any order, and all ids are unique. A node with availability REMOVED is excluded entirely along with none of its descendants; descendants remain if their own records are LIVE and connected through existing parents, otherwise they become new roots in the export forest. For every live root of the resulting forest, output one bundle line per node using preorder traversal among children sorted by title ascending then id ascending. Each line must have the exact form root_id::path::effective_rating where path is titles joined by '/', and effective_rating is the nearest non-UNKNOWN local rating on the path from the exported root to the node, or UNRATED if none exists. If no live nodes remain, return ['EMPTY::/::UNRATED'].\n\nFunction signature:\ndef build_rating_bundles(nodes: list[dict]) -> list[str]:\n\nInput:\nnodes is a list of dictionaries with keys: id (str), parent (str | None), title (str), rating (str), availability (str). rating may be UNKNOWN or another opaque token string.\n\nOutput:\nReturn a list of strings in exact root_id::path::effective_rating form, or the single placeholder string EMPTY::/::UNRATED when applicable.\n\nConstraints:\n- 1 <= len(nodes) <= 140000\n- All ids are unique\n- availability is either LIVE or REMOVED\n- title strings do not contain '/' or '::'\n\nReturn only code."}
{"id": "S01060", "language": "python", "difficulty": "hard", "problem_statement": "A spaceport tracks subsystem checks for each launch pad. Each record contains pad name, subsystem name, revision number, score as an integer from 0 to 100, and a tag from GREEN, AMBER, RED, or SKIP. For each (pad, subsystem), keep the record with the highest revision; ties use the last one in input order. A surviving SKIP record means the subsystem must still appear in the digest but with readiness text OMIT and score text -. Other tags produce readiness READY for GREEN, HOLD for AMBER, and BLOCK for RED, and keep the numeric score text unchanged. Output one line per pad in ascending pad name and subsystem name as pad::subsystem::readiness::score_text. If a pad has at least one surviving subsystem and all of them are SKIP, append one additional line after that pad's subsystem lines: pad::@::ALL_OMITTED::- . Pads seen only through malformed records with empty subsystem must instead produce exactly one line pad::@::NO_VALID_SUBSYSTEM::-.", "function_signature": "def build_pad_readiness_digest(records: list[dict]) -> list[str]:", "input_format": "records is a list of dictionaries with keys: pad (str), subsystem (str), rev (int), score (int), tag (str). Records with empty subsystem are malformed and cannot produce subsystem rows, but their pad still counts as seen.", "output_format": "Return a list of strings in exact pad::subsystem::readiness::score_text form, using subsystem @ only for pad-level summary rows.", "constraints": ["1 <= len(records) <= 150000", "0 <= score <= 100", "rev is an integer", "tag is one of GREEN, AMBER, RED, SKIP"], "solver_prompt": "Solve this problem in python.\n\nA spaceport tracks subsystem checks for each launch pad. Each record contains pad name, subsystem name, revision number, score as an integer from 0 to 100, and a tag from GREEN, AMBER, RED, or SKIP. For each (pad, subsystem), keep the record with the highest revision; ties use the last one in input order. A surviving SKIP record means the subsystem must still appear in the digest but with readiness text OMIT and score text -. Other tags produce readiness READY for GREEN, HOLD for AMBER, and BLOCK for RED, and keep the numeric score text unchanged. Output one line per pad in ascending pad name and subsystem name as pad::subsystem::readiness::score_text. If a pad has at least one surviving subsystem and all of them are SKIP, append one additional line after that pad's subsystem lines: pad::@::ALL_OMITTED::- . Pads seen only through malformed records with empty subsystem must instead produce exactly one line pad::@::NO_VALID_SUBSYSTEM::-.\n\nFunction signature:\ndef build_pad_readiness_digest(records: list[dict]) -> list[str]:\n\nInput:\nrecords is a list of dictionaries with keys: pad (str), subsystem (str), rev (int), score (int), tag (str). Records with empty subsystem are malformed and cannot produce subsystem rows, but their pad still counts as seen.\n\nOutput:\nReturn a list of strings in exact pad::subsystem::readiness::score_text form, using subsystem @ only for pad-level summary rows.\n\nConstraints:\n- 1 <= len(records) <= 150000\n- 0 <= score <= 100\n- rev is an integer\n- tag is one of GREEN, AMBER, RED, SKIP\n\nReturn only code."}
{"id": "S00005", "language": "python", "difficulty": "hard", "problem_statement": "A research campus models rooms as vertices and doors as undirected edges labeled with badge levels. A scientist may traverse a path if every door on that path has label at most the scientist's badge level. For many queries asking about two rooms, return the minimum badge level needed so that a path exists between them. The answer is defined by the highest-labeled door on the best path, not by the sum of labels, the number of doors, or the minimum spanning forest rooted at a particular source. Rooms and doors are static, and queries are independent except that they refer to the same graph.", "function_signature": "def min_badge_levels(n: int, doors: list[tuple[int, int, int]], queries: list[tuple[int, int]]) -> list[int]:", "input_format": "n is the number of rooms labeled 0 to n-1. doors contains (u, v, level) for each undirected door. queries contains pairs of room indices.", "output_format": "Return a list of integers, one per query, where each integer is the smallest badge level that permits travel between the queried rooms. If two rooms are disconnected, return -1 for that query.", "constraints": ["1 <= n <= 2 * 10^5", "0 <= len(doors) <= 3 * 10^5", "1 <= len(queries) <= 3 * 10^5", "1 <= level <= 10^12"], "solver_prompt": "Solve this problem in python.\n\nA research campus models rooms as vertices and doors as undirected edges labeled with badge levels. A scientist may traverse a path if every door on that path has label at most the scientist's badge level. For many queries asking about two rooms, return the minimum badge level needed so that a path exists between them. The answer is defined by the highest-labeled door on the best path, not by the sum of labels, the number of doors, or the minimum spanning forest rooted at a particular source. Rooms and doors are static, and queries are independent except that they refer to the same graph.\n\nFunction signature:\ndef min_badge_levels(n: int, doors: list[tuple[int, int, int]], queries: list[tuple[int, int]]) -> list[int]:\n\nInput:\nn is the number of rooms labeled 0 to n-1. doors contains (u, v, level) for each undirected door. queries contains pairs of room indices.\n\nOutput:\nReturn a list of integers, one per query, where each integer is the smallest badge level that permits travel between the queried rooms. If two rooms are disconnected, return -1 for that query.\n\nConstraints:\n- 1 <= n <= 2 * 10^5\n- 0 <= len(doors) <= 3 * 10^5\n- 1 <= len(queries) <= 3 * 10^5\n- 1 <= level <= 10^12\n\nReturn only code."}
{"id": "S00030", "language": "python", "difficulty": "hard", "problem_statement": "A university curriculum is a DAG of prerequisites among courses. In one term, a student may take any number of courses whose prerequisites were all completed in earlier terms; a course completed during the current term cannot unlock another course until the next term. Given the prerequisite graph and a target set of courses, compute the minimum number of terms needed to complete every target course, assuming the student starts with no courses completed and may also take non-target prerequisite courses when needed.", "function_signature": "def min_terms_for_targets(num_courses: int, prerequisites: list[tuple[int, int]], targets: list[int]) -> int:", "input_format": "num_courses gives course ids 1..num_courses. prerequisites contains pairs (a, b) meaning course a must be completed before course b. targets lists the required final courses.", "output_format": "Return the minimum number of academic terms required to complete all target courses.", "constraints": ["1 <= num_courses <= 250000", "0 <= len(prerequisites) <= 400000", "1 <= len(targets) <= num_courses", "The prerequisite graph is acyclic"], "solver_prompt": "Solve this problem in python.\n\nA university curriculum is a DAG of prerequisites among courses. In one term, a student may take any number of courses whose prerequisites were all completed in earlier terms; a course completed during the current term cannot unlock another course until the next term. Given the prerequisite graph and a target set of courses, compute the minimum number of terms needed to complete every target course, assuming the student starts with no courses completed and may also take non-target prerequisite courses when needed.\n\nFunction signature:\ndef min_terms_for_targets(num_courses: int, prerequisites: list[tuple[int, int]], targets: list[int]) -> int:\n\nInput:\nnum_courses gives course ids 1..num_courses. prerequisites contains pairs (a, b) meaning course a must be completed before course b. targets lists the required final courses.\n\nOutput:\nReturn the minimum number of academic terms required to complete all target courses.\n\nConstraints:\n- 1 <= num_courses <= 250000\n- 0 <= len(prerequisites) <= 400000\n- 1 <= len(targets) <= num_courses\n- The prerequisite graph is acyclic\n\nReturn only code."}
{"id": "S00001", "language": "python", "difficulty": "hard", "problem_statement": "A remote sensing system stores a heatmap as a height x width integer grid initially filled with zeros. You are given a long operation ledger. Each operation is one of: add a value to every cell in an axis-aligned rectangle, overwrite one specific cell with a new value, or copy the current contents of one row into another row. Operations must be applied in order, and a later change to one row must not retroactively alter any previously copied row. Return the final heatmap after all operations. The largest cases are sized so that a straightforward per-cell update for every rectangle operation will time out.", "function_signature": "def reconstruct_heatmap(height: int, width: int, ops: list[dict]) -> list[list[int]]:", "input_format": "height and width are the grid dimensions. ops is a list of dictionaries. An add-rectangle operation has {\"type\": \"add\", \"r1\": int, \"c1\": int, \"r2\": int, \"c2\": int, \"delta\": int} with inclusive coordinates. A set-cell operation has {\"type\": \"set\", \"r\": int, \"c\": int, \"value\": int}. A copy-row operation has {\"type\": \"copy_row\", \"src\": int, \"dst\": int}. Rows and columns are zero-indexed.", "output_format": "Return the final grid as a list of height lists, each containing width integers.", "constraints": ["1 <= height, width <= 2000", "1 <= len(ops) <= 100000", "0 <= r1 <= r2 < height", "0 <= c1 <= c2 < width", "-10^9 <= delta, value <= 10^9"], "solver_prompt": "Solve this problem in python.\n\nA remote sensing system stores a heatmap as a height x width integer grid initially filled with zeros. You are given a long operation ledger. Each operation is one of: add a value to every cell in an axis-aligned rectangle, overwrite one specific cell with a new value, or copy the current contents of one row into another row. Operations must be applied in order, and a later change to one row must not retroactively alter any previously copied row. Return the final heatmap after all operations. The largest cases are sized so that a straightforward per-cell update for every rectangle operation will time out.\n\nFunction signature:\ndef reconstruct_heatmap(height: int, width: int, ops: list[dict]) -> list[list[int]]:\n\nInput:\nheight and width are the grid dimensions. ops is a list of dictionaries. An add-rectangle operation has {\"type\": \"add\", \"r1\": int, \"c1\": int, \"r2\": int, \"c2\": int, \"delta\": int} with inclusive coordinates. A set-cell operation has {\"type\": \"set\", \"r\": int, \"c\": int, \"value\": int}. A copy-row operation has {\"type\": \"copy_row\", \"src\": int, \"dst\": int}. Rows and columns are zero-indexed.\n\nOutput:\nReturn the final grid as a list of height lists, each containing width integers.\n\nConstraints:\n- 1 <= height, width <= 2000\n- 1 <= len(ops) <= 100000\n- 0 <= r1 <= r2 < height\n- 0 <= c1 <= c2 < width\n- -10^9 <= delta, value <= 10^9\n\nReturn only code."}
{"id": "S00019", "language": "python", "difficulty": "hard", "problem_statement": "You are given many stored file paths and many templates. Paths use '/' as a separator and contain no empty segments except the leading root slash. In a template, the token '*' matches any sequence of characters within a single segment, while the token '**' matches zero or more whole segments. For each template, return how many stored paths match it exactly under these rules. Matching is based on path segments, not general regular-expression behavior over the full string.", "function_signature": "def count_matching_paths(paths: list[str], templates: list[str]) -> list[int]:", "input_format": "paths is the list of absolute stored paths. templates is the list of absolute path templates using literal text, '*', and '**'.", "output_format": "Return a list of counts, one for each template.", "constraints": ["1 <= len(paths) <= 3 * 10^5", "1 <= len(templates) <= 3 * 10^5", "Total length of all path strings <= 3 * 10^6", "Each path and template begins with '/'"], "solver_prompt": "Solve this problem in python.\n\nYou are given many stored file paths and many templates. Paths use '/' as a separator and contain no empty segments except the leading root slash. In a template, the token '*' matches any sequence of characters within a single segment, while the token '**' matches zero or more whole segments. For each template, return how many stored paths match it exactly under these rules. Matching is based on path segments, not general regular-expression behavior over the full string.\n\nFunction signature:\ndef count_matching_paths(paths: list[str], templates: list[str]) -> list[int]:\n\nInput:\npaths is the list of absolute stored paths. templates is the list of absolute path templates using literal text, '*', and '**'.\n\nOutput:\nReturn a list of counts, one for each template.\n\nConstraints:\n- 1 <= len(paths) <= 3 * 10^5\n- 1 <= len(templates) <= 3 * 10^5\n- Total length of all path strings <= 3 * 10^6\n- Each path and template begins with '/'\n\nReturn only code."}
{"id": "S01051", "language": "python", "difficulty": "hard", "problem_statement": "A city stores hydrant inspection updates as records containing district code, hydrant id, inspection date in YYYY-MM-DD format, inspector badge, and a result token from the set PASS, FAIL, REPAIR, RETIRED. Multiple records may exist for the same hydrant on the same date, and only the last record in input order for that date is valid. For each district that appears at least once, produce a roster of hydrants ordered by hydrant id. For each hydrant, keep only its latest inspection date overall after same-day collapse. The exported status is ACTIVE for PASS or REPAIR, BLOCKED for FAIL, and CLOSED for RETIRED. The output must be a list of strings in the exact form district|hydrant|date|status|inspector, except that retired hydrants must use inspector=* regardless of the source badge. Districts with seen records but no surviving hydrants after applying removals must still contribute one row as district|NONE|0000-00-00|EMPTY|*.", "function_signature": "def export_hydrant_roster(records: list[dict]) -> list[str]:", "input_format": "records is a list of dictionaries with keys: district (str), hydrant (str), date (str), inspector (str), result (str). Input order is significant for resolving duplicates on the same hydrant and date.", "output_format": "Return a list of strings. Each string must be exactly district|hydrant|date|status|inspector. Emit one EMPTY row for a district only if that district appears in input but has no surviving hydrant row.", "constraints": ["1 <= len(records) <= 180000", "district, hydrant, and inspector are non-empty ASCII strings without '|'", "date is a valid calendar date string in YYYY-MM-DD", "result is one of PASS, FAIL, REPAIR, RETIRED"], "solver_prompt": "Solve this problem in python.\n\nA city stores hydrant inspection updates as records containing district code, hydrant id, inspection date in YYYY-MM-DD format, inspector badge, and a result token from the set PASS, FAIL, REPAIR, RETIRED. Multiple records may exist for the same hydrant on the same date, and only the last record in input order for that date is valid. For each district that appears at least once, produce a roster of hydrants ordered by hydrant id. For each hydrant, keep only its latest inspection date overall after same-day collapse. The exported status is ACTIVE for PASS or REPAIR, BLOCKED for FAIL, and CLOSED for RETIRED. The output must be a list of strings in the exact form district|hydrant|date|status|inspector, except that retired hydrants must use inspector=* regardless of the source badge. Districts with seen records but no surviving hydrants after applying removals must still contribute one row as district|NONE|0000-00-00|EMPTY|*.\n\nFunction signature:\ndef export_hydrant_roster(records: list[dict]) -> list[str]:\n\nInput:\nrecords is a list of dictionaries with keys: district (str), hydrant (str), date (str), inspector (str), result (str). Input order is significant for resolving duplicates on the same hydrant and date.\n\nOutput:\nReturn a list of strings. Each string must be exactly district|hydrant|date|status|inspector. Emit one EMPTY row for a district only if that district appears in input but has no surviving hydrant row.\n\nConstraints:\n- 1 <= len(records) <= 180000\n- district, hydrant, and inspector are non-empty ASCII strings without '|'\n- date is a valid calendar date string in YYYY-MM-DD\n- result is one of PASS, FAIL, REPAIR, RETIRED\n\nReturn only code."}
{"id": "S00042", "language": "python", "difficulty": "hard", "problem_statement": "A cloud provider records each compute instance run as `(account_id, start_minute, end_minute, weight)`, meaning the instance contributes `weight` units of capacity on the half-open interval `[start_minute, end_minute)`. Billing is not based on runtime totals. For each account and for each UTC day, define that day's billed capacity as the maximum total active weight at any minute during that day. The bill for that day is `billed_capacity * daily_rate`. Runs that cross midnight contribute separately to each day they overlap. Given all runs and a single integer `daily_rate`, compute the total bill for every account.", "function_signature": "def bill_burst_capacity(runs: list[tuple[str, int, int, int]], daily_rate: int) -> dict[str, int]:", "input_format": "A list of records `(account_id, start_minute, end_minute, weight)` in arbitrary order, where times are integer minutes from a fixed epoch, and an integer `daily_rate` applied uniformly to all accounts and days.", "output_format": "Return a dictionary mapping each account_id to its total billed amount.", "constraints": ["0 <= len(runs) <= 9e5", "0 <= start_minute < end_minute <= 10^9", "1 <= weight <= 10^6", "1 <= daily_rate <= 10^6"], "solver_prompt": "Solve this problem in python.\n\nA cloud provider records each compute instance run as `(account_id, start_minute, end_minute, weight)`, meaning the instance contributes `weight` units of capacity on the half-open interval `[start_minute, end_minute)`. Billing is not based on runtime totals. For each account and for each UTC day, define that day's billed capacity as the maximum total active weight at any minute during that day. The bill for that day is `billed_capacity * daily_rate`. Runs that cross midnight contribute separately to each day they overlap. Given all runs and a single integer `daily_rate`, compute the total bill for every account.\n\nFunction signature:\ndef bill_burst_capacity(runs: list[tuple[str, int, int, int]], daily_rate: int) -> dict[str, int]:\n\nInput:\nA list of records `(account_id, start_minute, end_minute, weight)` in arbitrary order, where times are integer minutes from a fixed epoch, and an integer `daily_rate` applied uniformly to all accounts and days.\n\nOutput:\nReturn a dictionary mapping each account_id to its total billed amount.\n\nConstraints:\n- 0 <= len(runs) <= 9e5\n- 0 <= start_minute < end_minute <= 10^9\n- 1 <= weight <= 10^6\n- 1 <= daily_rate <= 10^6\n\nReturn only code."}
{"id": "S70011", "language": "cpp", "difficulty": "hard", "problem_statement": "A gateway fleet stores TLS certificates by symbolic name. Over time, a certificate can be created, replaced by a newer certificate for the same name, explicitly revoked, or duplicated into a temporary alias that later becomes independent. Client requests reference certificate handles rather than names, and a request is accepted only if the referenced handle is currently valid at that moment. Process up to 300,000 operations and return, for every VERIFY operation, whether the request should be accepted. Replacement and aliasing change which handle is active for future lookups, but previously issued handles do not automatically become current again after later revocations or rollovers.", "function_signature": "std::vector<std::string> evaluate_certificate_ops(const std::vector<std::string>& operations);", "input_format": "Each string is one command. Commands are: ISSUE name handle, ROLL name newHandle, REVOKE handle, ALIAS srcHandle aliasHandle, SPLIT aliasHandle newName, VERIFY handle. Names and handles are case-sensitive alphanumeric strings without spaces.", "output_format": "Return a vector containing one element per VERIFY command, each being \"ACCEPT\" or \"REJECT\".", "constraints": ["1 <= operations.size() <= 300000", "Total length of all command strings <= 8e6", "Each handle token length is between 1 and 24"], "solver_prompt": "Solve this problem in cpp.\n\nA gateway fleet stores TLS certificates by symbolic name. Over time, a certificate can be created, replaced by a newer certificate for the same name, explicitly revoked, or duplicated into a temporary alias that later becomes independent. Client requests reference certificate handles rather than names, and a request is accepted only if the referenced handle is currently valid at that moment. Process up to 300,000 operations and return, for every VERIFY operation, whether the request should be accepted. Replacement and aliasing change which handle is active for future lookups, but previously issued handles do not automatically become current again after later revocations or rollovers.\n\nFunction signature:\nstd::vector<std::string> evaluate_certificate_ops(const std::vector<std::string>& operations);\n\nInput:\nEach string is one command. Commands are: ISSUE name handle, ROLL name newHandle, REVOKE handle, ALIAS srcHandle aliasHandle, SPLIT aliasHandle newName, VERIFY handle. Names and handles are case-sensitive alphanumeric strings without spaces.\n\nOutput:\nReturn a vector containing one element per VERIFY command, each being \"ACCEPT\" or \"REJECT\".\n\nConstraints:\n- 1 <= operations.size() <= 300000\n- Total length of all command strings <= 8e6\n- Each handle token length is between 1 and 24\n\nReturn only code."}
{"id": "S00031", "language": "cpp", "difficulty": "hard", "problem_statement": "You are given a binary image of farmland captured as equal-length strings of '0' and '1'. A fixed square block size B partitions the image into non-overlapping B x B patches. Many patches repeat exactly. Build an index over all patches, then answer rectangle queries whose borders align to patch boundaries. For each query, return how many distinct patch patterns appear inside the queried patch rectangle. The judge creates the index through the required construction entrypoint and then performs many read-only queries on the built object.", "function_signature": "class MosaicIndex { public: static MosaicIndex build(const vector<string>& grid, int blockSize); int distinctInWindow(int topBlock, int leftBlock, int bottomBlock, int rightBlock) const; };", "input_format": "The image is provided to build as a vector<string> grid with equal widths and characters '0' or '1'. blockSize is a positive integer dividing both dimensions. Each query to distinctInWindow uses inclusive block coordinates.", "output_format": "For each query method call, return a single integer equal to the number of distinct B x B patch contents fully contained in the queried block rectangle.", "constraints": ["1 <= H, W <= 1200", "1 <= B <= min(H, W)", "B divides H and W", "1 <= number of patches <= 1.44e6", "1 <= number of queries <= 9e4"], "solver_prompt": "Solve this problem in cpp.\n\nYou are given a binary image of farmland captured as equal-length strings of '0' and '1'. A fixed square block size B partitions the image into non-overlapping B x B patches. Many patches repeat exactly. Build an index over all patches, then answer rectangle queries whose borders align to patch boundaries. For each query, return how many distinct patch patterns appear inside the queried patch rectangle. The judge creates the index through the required construction entrypoint and then performs many read-only queries on the built object.\n\nFunction signature:\nclass MosaicIndex { public: static MosaicIndex build(const vector<string>& grid, int blockSize); int distinctInWindow(int topBlock, int leftBlock, int bottomBlock, int rightBlock) const; };\n\nInput:\nThe image is provided to build as a vector<string> grid with equal widths and characters '0' or '1'. blockSize is a positive integer dividing both dimensions. Each query to distinctInWindow uses inclusive block coordinates.\n\nOutput:\nFor each query method call, return a single integer equal to the number of distinct B x B patch contents fully contained in the queried block rectangle.\n\nConstraints:\n- 1 <= H, W <= 1200\n- 1 <= B <= min(H, W)\n- B divides H and W\n- 1 <= number of patches <= 1.44e6\n- 1 <= number of queries <= 9e4\n\nReturn only code."}
{"id": "S01057", "language": "python", "difficulty": "hard", "problem_statement": "A chain of currency kiosks reports denomination counts per closing batch. Each record contains kiosk id, currency code, denomination value in minor units, batch number, counted quantity, and a seal status VERIFIED, BROKEN, or VOID. For each (kiosk, currency, denomination), keep the record with the largest batch number; ties use the last record in input order. The exported quantity text depends on status: VERIFIED uses the integer quantity as text, BROKEN uses ? instead of the number, and VOID removes that denomination from the report. For each (kiosk, currency) that appears in input, output rows sorted by denomination descending for surviving denominations in the exact form kiosk,currency,denomination,quantity_text,total_value where total_value is denomination*quantity for VERIFIED rows and NA for BROKEN rows. If a (kiosk, currency) group has no surviving denomination rows, emit kiosk,currency,0,EMPTY,0.", "function_signature": "def reconcile_kiosk_batches(records: list[dict]) -> list[str]:", "input_format": "records is a list of dictionaries with keys: kiosk (str), currency (str), denom (int), batch (int), qty (int), seal (str). Input order resolves ties on batch.", "output_format": "Return a list of comma-separated strings in exact kiosk,currency,denomination,quantity_text,total_value form.", "constraints": ["1 <= len(records) <= 260000", "denom and qty are non-negative integers", "batch is a non-negative integer", "seal is one of VERIFIED, BROKEN, VOID"], "solver_prompt": "Solve this problem in python.\n\nA chain of currency kiosks reports denomination counts per closing batch. Each record contains kiosk id, currency code, denomination value in minor units, batch number, counted quantity, and a seal status VERIFIED, BROKEN, or VOID. For each (kiosk, currency, denomination), keep the record with the largest batch number; ties use the last record in input order. The exported quantity text depends on status: VERIFIED uses the integer quantity as text, BROKEN uses ? instead of the number, and VOID removes that denomination from the report. For each (kiosk, currency) that appears in input, output rows sorted by denomination descending for surviving denominations in the exact form kiosk,currency,denomination,quantity_text,total_value where total_value is denomination*quantity for VERIFIED rows and NA for BROKEN rows. If a (kiosk, currency) group has no surviving denomination rows, emit kiosk,currency,0,EMPTY,0.\n\nFunction signature:\ndef reconcile_kiosk_batches(records: list[dict]) -> list[str]:\n\nInput:\nrecords is a list of dictionaries with keys: kiosk (str), currency (str), denom (int), batch (int), qty (int), seal (str). Input order resolves ties on batch.\n\nOutput:\nReturn a list of comma-separated strings in exact kiosk,currency,denomination,quantity_text,total_value form.\n\nConstraints:\n- 1 <= len(records) <= 260000\n- denom and qty are non-negative integers\n- batch is a non-negative integer\n- seal is one of VERIFIED, BROKEN, VOID\n\nReturn only code."}
{"id": "S00038", "language": "cpp", "difficulty": "hard", "problem_statement": "A legal archive stores tokenized documents and a set of rewrite rules that expand abbreviations, normalize reporter names, and resolve short-form citations to the nearest earlier compatible full citation in the same document. For each document, emit the normalized token sequence together with the positions of tokens that changed. The judge expects a namespace-level alias for the returned batch shape and a templated entry function that works for any string-like token type supporting equality and concatenation.", "function_signature": "namespace archive_api { template <class Token> using NormalizedBatch = vector<pair<vector<Token>, vector<int>>>; template <class Token> NormalizedBatch<Token> normalize_citations(const vector<vector<Token>>& docs, const vector<pair<vector<Token>, vector<Token>>>& rewriteRules); }", "input_format": "docs is a list of token sequences, one per document. rewriteRules maps a source token sequence to a replacement token sequence. Short-form resolution is document-local and uses the nearest earlier normalized citation with the same first token and final numeric token.", "output_format": "Return one pair per document. The first component is the normalized token sequence. The second component is the sorted list of token indices in the output sequence that originate from any rewrite or short-form expansion.", "constraints": ["1 <= number of documents <= 2e4", "1 <= total tokens across all docs <= 8e5", "1 <= number of rewrite rules <= 1e5", "sum of rule token lengths <= 8e5"], "solver_prompt": "Solve this problem in cpp.\n\nA legal archive stores tokenized documents and a set of rewrite rules that expand abbreviations, normalize reporter names, and resolve short-form citations to the nearest earlier compatible full citation in the same document. For each document, emit the normalized token sequence together with the positions of tokens that changed. The judge expects a namespace-level alias for the returned batch shape and a templated entry function that works for any string-like token type supporting equality and concatenation.\n\nFunction signature:\nnamespace archive_api { template <class Token> using NormalizedBatch = vector<pair<vector<Token>, vector<int>>>; template <class Token> NormalizedBatch<Token> normalize_citations(const vector<vector<Token>>& docs, const vector<pair<vector<Token>, vector<Token>>>& rewriteRules); }\n\nInput:\ndocs is a list of token sequences, one per document. rewriteRules maps a source token sequence to a replacement token sequence. Short-form resolution is document-local and uses the nearest earlier normalized citation with the same first token and final numeric token.\n\nOutput:\nReturn one pair per document. The first component is the normalized token sequence. The second component is the sorted list of token indices in the output sequence that originate from any rewrite or short-form expansion.\n\nConstraints:\n- 1 <= number of documents <= 2e4\n- 1 <= total tokens across all docs <= 8e5\n- 1 <= number of rewrite rules <= 1e5\n- sum of rule token lengths <= 8e5\n\nReturn only code."}
{"id": "S00026", "language": "python", "difficulty": "hard", "problem_statement": "A media archive stores a stream of tokens for each video transcript. A chapter title is defined as the shortest contiguous segment that starts with a token in a given start set and ends with a token in a given end set while containing every required keyword at least once somewhere inside the same segment. For each transcript, return one valid shortest chapter segment as token indices, or (-1, -1) if none exists. The shortest segment is chosen by length first and then by smallest starting index.", "function_signature": "def extract_shortest_chapter(transcripts: list[list[str]], start_tokens: set[str], end_tokens: set[str], required_keywords: set[str]) -> list[tuple[int, int]]:", "input_format": "transcripts is a list of token lists, one per video. start_tokens and end_tokens define valid boundary tokens. required_keywords contains the tokens that must all appear within the chosen segment.", "output_format": "Return for each transcript a pair (l, r) of 0-based inclusive indices, or (-1, -1) if no valid segment exists.", "constraints": ["1 <= total number of tokens across all transcripts <= 800000", "1 <= len(required_keywords) <= 20", "Token strings are non-empty ASCII words"], "solver_prompt": "Solve this problem in python.\n\nA media archive stores a stream of tokens for each video transcript. A chapter title is defined as the shortest contiguous segment that starts with a token in a given start set and ends with a token in a given end set while containing every required keyword at least once somewhere inside the same segment. For each transcript, return one valid shortest chapter segment as token indices, or (-1, -1) if none exists. The shortest segment is chosen by length first and then by smallest starting index.\n\nFunction signature:\ndef extract_shortest_chapter(transcripts: list[list[str]], start_tokens: set[str], end_tokens: set[str], required_keywords: set[str]) -> list[tuple[int, int]]:\n\nInput:\ntranscripts is a list of token lists, one per video. start_tokens and end_tokens define valid boundary tokens. required_keywords contains the tokens that must all appear within the chosen segment.\n\nOutput:\nReturn for each transcript a pair (l, r) of 0-based inclusive indices, or (-1, -1) if no valid segment exists.\n\nConstraints:\n- 1 <= total number of tokens across all transcripts <= 800000\n- 1 <= len(required_keywords) <= 20\n- Token strings are non-empty ASCII words\n\nReturn only code."}
{"id": "S01072", "language": "python", "difficulty": "hard", "problem_statement": "An archive stores sparse style directives for character positions in citation ribbons. Each update targets a ribbon id, a closed interval [l, r], a style token, and a priority. For each ribbon, the final style at a position is chosen from the update with highest priority covering that position; ties are broken by later input order. Build a canonical run list for every ribbon from position 1 through its declared length. Consecutive positions with the same final style must be merged into one run. Uncovered positions are not discarded: they must appear using the literal style token \"PLAIN\". Output runs in ascending ribbon order, then by run start. Every run must render the inclusive endpoints exactly as stored.", "function_signature": "def compile_citation_ribbons(ribbons: list[dict], updates: list[dict]) -> list[str]:", "input_format": "ribbons is a list of dictionaries with keys: ribbon_id (str), length (int). updates is a list of dictionaries with keys: ribbon_id (str), l (int), r (int), style (str), priority (int).", "output_format": "Return a list of strings in the form \"ribbon_id|start|end|style\" describing merged inclusive runs for each ribbon.", "constraints": ["1 <= len(ribbons) <= 10^5", "0 <= len(updates) <= 2 * 10^5", "1 <= length <= 10^18", "1 <= l <= r <= length of the referenced ribbon"], "solver_prompt": "Solve this problem in python.\n\nAn archive stores sparse style directives for character positions in citation ribbons. Each update targets a ribbon id, a closed interval [l, r], a style token, and a priority. For each ribbon, the final style at a position is chosen from the update with highest priority covering that position; ties are broken by later input order. Build a canonical run list for every ribbon from position 1 through its declared length. Consecutive positions with the same final style must be merged into one run. Uncovered positions are not discarded: they must appear using the literal style token \"PLAIN\". Output runs in ascending ribbon order, then by run start. Every run must render the inclusive endpoints exactly as stored.\n\nFunction signature:\ndef compile_citation_ribbons(ribbons: list[dict], updates: list[dict]) -> list[str]:\n\nInput:\nribbons is a list of dictionaries with keys: ribbon_id (str), length (int). updates is a list of dictionaries with keys: ribbon_id (str), l (int), r (int), style (str), priority (int).\n\nOutput:\nReturn a list of strings in the form \"ribbon_id|start|end|style\" describing merged inclusive runs for each ribbon.\n\nConstraints:\n- 1 <= len(ribbons) <= 10^5\n- 0 <= len(updates) <= 2 * 10^5\n- 1 <= length <= 10^18\n- 1 <= l <= r <= length of the referenced ribbon\n\nReturn only code."}
{"id": "S01002", "language": "python", "difficulty": "hard", "problem_statement": "A bioinformatics pipeline receives a chromosome length and a large set of half-open intervals tagged as masked or unmasked updates. Starting from an all-unmasked chromosome, apply updates in the given order so later intervals override earlier ones on overlap. After all updates, compress the final chromosome state into maximal runs and return only the masked runs. The output must omit zero-length runs, merge adjacent runs that end up masked continuously, and represent coordinates in ascending order. Interval endpoints may lie on the chromosome boundary, and updates can fully cover, split, or erase earlier masked regions.", "function_signature": "def masked_runs(length: int, updates: list[tuple[int, int, int]]) -> list[tuple[int, int]]:", "input_format": "An integer length, and a list of tuples (start, end, flag) where 0 <= start <= end <= length and flag is 0 for unmasked or 1 for masked. Intervals are half-open: [start, end).", "output_format": "Return a list of (start, end) tuples for maximal masked runs, sorted by start.", "constraints": ["1 <= length <= 10^18", "1 <= len(updates) <= 200000", "Coordinates fit in signed 64-bit integers"], "solver_prompt": "Solve this problem in python.\n\nA bioinformatics pipeline receives a chromosome length and a large set of half-open intervals tagged as masked or unmasked updates. Starting from an all-unmasked chromosome, apply updates in the given order so later intervals override earlier ones on overlap. After all updates, compress the final chromosome state into maximal runs and return only the masked runs. The output must omit zero-length runs, merge adjacent runs that end up masked continuously, and represent coordinates in ascending order. Interval endpoints may lie on the chromosome boundary, and updates can fully cover, split, or erase earlier masked regions.\n\nFunction signature:\ndef masked_runs(length: int, updates: list[tuple[int, int, int]]) -> list[tuple[int, int]]:\n\nInput:\nAn integer length, and a list of tuples (start, end, flag) where 0 <= start <= end <= length and flag is 0 for unmasked or 1 for masked. Intervals are half-open: [start, end).\n\nOutput:\nReturn a list of (start, end) tuples for maximal masked runs, sorted by start.\n\nConstraints:\n- 1 <= length <= 10^18\n- 1 <= len(updates) <= 200000\n- Coordinates fit in signed 64-bit integers\n\nReturn only code."}
{"id": "S01041", "language": "python", "difficulty": "hard", "problem_statement": "A remote sensing pipeline receives many tile ingests for the same scene. Each ingest record contains a scene_id, band name, zoom level, row, column, revision number, checksum, and a status string that is either \"ok\" or \"drop\". For every unique tile identified by (scene_id, band, zoom, row, column), keep only the highest revision among records whose status is \"ok\". If there is no \"ok\" record for a tile, that tile must still appear in the output with checksum set to the literal string \"MISSING\" and revision set to -1. Return one manifest line per tile using the exact grammar \"scene|band|z{zoom}|r{row}|c{col}|rev={revision}|sum={checksum}\". Lines must be ordered by scene_id ascending, then zoom descending, then band ascending, then row ascending, then column ascending.", "function_signature": "def build_tile_manifest(records: list[dict]) -> list[str]:", "input_format": "records is a list of dictionaries with keys: scene_id (str), band (str), zoom (int), row (int), col (int), revision (int), checksum (str), status (str). Multiple records may describe the same tile.", "output_format": "Return a list of strings, each exactly formatted as \"scene|band|z{zoom}|r{row}|c{col}|rev={revision}|sum={checksum}\".", "constraints": ["1 <= len(records) <= 200000", "0 <= zoom <= 30", "0 <= row, col <= 10^7", "revision values fit in 32-bit signed integer"], "solver_prompt": "Solve this problem in python.\n\nA remote sensing pipeline receives many tile ingests for the same scene. Each ingest record contains a scene_id, band name, zoom level, row, column, revision number, checksum, and a status string that is either \"ok\" or \"drop\". For every unique tile identified by (scene_id, band, zoom, row, column), keep only the highest revision among records whose status is \"ok\". If there is no \"ok\" record for a tile, that tile must still appear in the output with checksum set to the literal string \"MISSING\" and revision set to -1. Return one manifest line per tile using the exact grammar \"scene|band|z{zoom}|r{row}|c{col}|rev={revision}|sum={checksum}\". Lines must be ordered by scene_id ascending, then zoom descending, then band ascending, then row ascending, then column ascending.\n\nFunction signature:\ndef build_tile_manifest(records: list[dict]) -> list[str]:\n\nInput:\nrecords is a list of dictionaries with keys: scene_id (str), band (str), zoom (int), row (int), col (int), revision (int), checksum (str), status (str). Multiple records may describe the same tile.\n\nOutput:\nReturn a list of strings, each exactly formatted as \"scene|band|z{zoom}|r{row}|c{col}|rev={revision}|sum={checksum}\".\n\nConstraints:\n- 1 <= len(records) <= 200000\n- 0 <= zoom <= 30\n- 0 <= row, col <= 10^7\n- revision values fit in 32-bit signed integer\n\nReturn only code."}
{"id": "S70005", "language": "cpp", "difficulty": "hard", "problem_statement": "An airport scheduler receives flights in chronological order with updates that assign a gate, delay a flight, cancel a flight, merge a delayed flight into a replacement flight id, and ask which active flight occupies a gate at a specific time checkpoint. After a merge, the old flight id is retired and all future updates that mention it should affect the replacement flight if and only if the old flight was still the current live representative at the time of merge. Implement a processor that answers all gate queries based on the active schedule after applying every prior update. Gate occupancy intervals may overlap in the input, and newer valid assignments replace earlier ones.", "function_signature": "std::vector<int> resolveGateQueries(const std::vector<std::string>& updates);", "input_format": "Each update string has one of these forms: \"ASSIGN flight gate start end\", \"DELAY flight delta\", \"CANCEL flight\", \"MERGE old_flight new_flight\", or \"ASK gate time\". Times and ids are integers.", "output_format": "Return the active flight id for each ASK operation, or -1 if no active flight occupies that gate at that time.", "constraints": ["1 <= updates.size() <= 200000", "0 <= start <= end <= 1000000000", "Gate names are integers"], "solver_prompt": "Solve this problem in cpp.\n\nAn airport scheduler receives flights in chronological order with updates that assign a gate, delay a flight, cancel a flight, merge a delayed flight into a replacement flight id, and ask which active flight occupies a gate at a specific time checkpoint. After a merge, the old flight id is retired and all future updates that mention it should affect the replacement flight if and only if the old flight was still the current live representative at the time of merge. Implement a processor that answers all gate queries based on the active schedule after applying every prior update. Gate occupancy intervals may overlap in the input, and newer valid assignments replace earlier ones.\n\nFunction signature:\nstd::vector<int> resolveGateQueries(const std::vector<std::string>& updates);\n\nInput:\nEach update string has one of these forms: \"ASSIGN flight gate start end\", \"DELAY flight delta\", \"CANCEL flight\", \"MERGE old_flight new_flight\", or \"ASK gate time\". Times and ids are integers.\n\nOutput:\nReturn the active flight id for each ASK operation, or -1 if no active flight occupies that gate at that time.\n\nConstraints:\n- 1 <= updates.size() <= 200000\n- 0 <= start <= end <= 1000000000\n- Gate names are integers\n\nReturn only code."}
{"id": "S00011", "language": "cpp", "difficulty": "hard", "problem_statement": "A city transit feed emits updates for many trips. Each update contains a trip identifier, a stop index along that trip, and a signed delay in seconds recorded at that stop. For every trip, produce a compact summary consisting of the maximum delay ever seen on the trip, the final delay after processing all updates in chronological order, and the count of stops whose delay was strictly positive at their latest known reading. Trips may appear in any order, and multiple updates may target the same stop. Return summaries sorted by trip identifier.", "function_signature": "class TransitAnalyzer { public: enum class Status { OnTime, Delayed, Cancelled }; static std::vector<std::array<int,4>> summarize(const std::vector<std::array<int,4>>& updates); };", "input_format": "Each element of updates is {time, trip_id, stop_index, delay_seconds}. The vector is not guaranteed to be grouped by trip, but times are strictly increasing overall.", "output_format": "Return a vector where each element is {trip_id, max_delay_seen, final_delay_at_last_updated_stop_time, positive_stop_count}, sorted by trip_id ascending.", "constraints": ["1 <= updates.size() <= 100000", "0 <= trip_id <= 1000000000", "0 <= stop_index <= 1000000", "-100000 <= delay_seconds <= 100000"], "solver_prompt": "Solve this problem in cpp.\n\nA city transit feed emits updates for many trips. Each update contains a trip identifier, a stop index along that trip, and a signed delay in seconds recorded at that stop. For every trip, produce a compact summary consisting of the maximum delay ever seen on the trip, the final delay after processing all updates in chronological order, and the count of stops whose delay was strictly positive at their latest known reading. Trips may appear in any order, and multiple updates may target the same stop. Return summaries sorted by trip identifier.\n\nFunction signature:\nclass TransitAnalyzer { public: enum class Status { OnTime, Delayed, Cancelled }; static std::vector<std::array<int,4>> summarize(const std::vector<std::array<int,4>>& updates); };\n\nInput:\nEach element of updates is {time, trip_id, stop_index, delay_seconds}. The vector is not guaranteed to be grouped by trip, but times are strictly increasing overall.\n\nOutput:\nReturn a vector where each element is {trip_id, max_delay_seen, final_delay_at_last_updated_stop_time, positive_stop_count}, sorted by trip_id ascending.\n\nConstraints:\n- 1 <= updates.size() <= 100000\n- 0 <= trip_id <= 1000000000\n- 0 <= stop_index <= 1000000\n- -100000 <= delay_seconds <= 100000\n\nReturn only code."}
{"id": "S00001", "language": "python", "difficulty": "hard", "problem_statement": "A deep-space network contains n relay stations numbered from 0 to n-1 and m directed links. Each link from u to v has a signal delay d and an activation energy cost c. A transmission from source s to target t may use any directed path whose total delay is at most D. Among all such feasible paths, return the minimum possible maximum single-link activation cost used on the path. If no path satisfies the delay limit, return -1. The graph can be sparse or dense enough that approaches based on enumerating all paths or treating delay and energy as a single additive weight will fail on larger tests. The function must handle repeated edges, self-loops, and disconnected regions.", "function_signature": "def min_max_link_cost(n: int, links: list[tuple[int, int, int, int]], s: int, t: int, D: int) -> int:", "input_format": "n is the number of stations. links contains tuples (u, v, delay, cost). s is the source station, t is the target station, and D is the maximum allowed total delay.", "output_format": "Return the smallest possible value of the maximum edge cost on a valid path from s to t whose total delay does not exceed D, or -1 if no such path exists.", "constraints": ["2 <= n <= 2 * 10^5", "0 <= len(links) <= 3 * 10^5", "0 <= u, v < n", "1 <= delay, cost <= 10^9", "0 <= s, t < n", "1 <= D <= 10^14"], "solver_prompt": "Solve this problem in python.\n\nA deep-space network contains n relay stations numbered from 0 to n-1 and m directed links. Each link from u to v has a signal delay d and an activation energy cost c. A transmission from source s to target t may use any directed path whose total delay is at most D. Among all such feasible paths, return the minimum possible maximum single-link activation cost used on the path. If no path satisfies the delay limit, return -1. The graph can be sparse or dense enough that approaches based on enumerating all paths or treating delay and energy as a single additive weight will fail on larger tests. The function must handle repeated edges, self-loops, and disconnected regions.\n\nFunction signature:\ndef min_max_link_cost(n: int, links: list[tuple[int, int, int, int]], s: int, t: int, D: int) -> int:\n\nInput:\nn is the number of stations. links contains tuples (u, v, delay, cost). s is the source station, t is the target station, and D is the maximum allowed total delay.\n\nOutput:\nReturn the smallest possible value of the maximum edge cost on a valid path from s to t whose total delay does not exceed D, or -1 if no such path exists.\n\nConstraints:\n- 2 <= n <= 2 * 10^5\n- 0 <= len(links) <= 3 * 10^5\n- 0 <= u, v < n\n- 1 <= delay, cost <= 10^9\n- 0 <= s, t < n\n- 1 <= D <= 10^14\n\nReturn only code."}
{"id": "S01040", "language": "python", "difficulty": "hard", "problem_statement": "A post-production tool receives subtitle cues as `(language, speaker, start_ms, end_ms, text)`. For each language, merge consecutive cues from the same speaker when the next cue starts exactly where the previous one ends and the normalized text matches exactly after trimming outer spaces. Do not merge overlapping cues or cues separated by gaps. Return a dictionary mapping each language to a list of export strings sorted by start time. Every export string must be `speaker|start-end|text`, with times printed as raw milliseconds. If a language has no cues after removing records whose trimmed text is empty, its value must be a one-element list containing the exact string `EMPTY`. Languages present in the input must always appear as keys in the returned dictionary.", "function_signature": "def merge_subtitle_cues(cues: list[tuple[str, str, int, int, str]]) -> dict[str, list[str]]:", "input_format": "A list of tuples `(language, speaker, start_ms, end_ms, text)` in arbitrary order. Trimming means removing leading and trailing ASCII spaces from `text` before emptiness checks and equality checks for merging.", "output_format": "A dictionary from language to a list of export strings, or `['EMPTY']` for languages whose usable cues vanish after trimming.", "constraints": ["1 <= len(cues) <= 200000", "0 <= start_ms < end_ms <= 10^12", "Language and speaker are non-empty strings", "Text may contain internal spaces"], "solver_prompt": "Solve this problem in python.\n\nA post-production tool receives subtitle cues as `(language, speaker, start_ms, end_ms, text)`. For each language, merge consecutive cues from the same speaker when the next cue starts exactly where the previous one ends and the normalized text matches exactly after trimming outer spaces. Do not merge overlapping cues or cues separated by gaps. Return a dictionary mapping each language to a list of export strings sorted by start time. Every export string must be `speaker|start-end|text`, with times printed as raw milliseconds. If a language has no cues after removing records whose trimmed text is empty, its value must be a one-element list containing the exact string `EMPTY`. Languages present in the input must always appear as keys in the returned dictionary.\n\nFunction signature:\ndef merge_subtitle_cues(cues: list[tuple[str, str, int, int, str]]) -> dict[str, list[str]]:\n\nInput:\nA list of tuples `(language, speaker, start_ms, end_ms, text)` in arbitrary order. Trimming means removing leading and trailing ASCII spaces from `text` before emptiness checks and equality checks for merging.\n\nOutput:\nA dictionary from language to a list of export strings, or `['EMPTY']` for languages whose usable cues vanish after trimming.\n\nConstraints:\n- 1 <= len(cues) <= 200000\n- 0 <= start_ms < end_ms <= 10^12\n- Language and speaker are non-empty strings\n- Text may contain internal spaces\n\nReturn only code."}
{"id": "S70001", "language": "cpp", "difficulty": "hard", "problem_statement": "A backend service receives a chronological stream of operations that create user sessions, refresh them, attach aliases, revoke them, and query the currently valid owner for a token. A session may be replaced by a newer session for the same account, aliases may be reassigned, and revocation of a session must also invalidate every token currently bound to it. Implement the processor so that each query returns the visible account id at that time or -1 if the token is no longer valid. Tokens and aliases can appear in operations before they are ever created, and repeated revocations or refreshes must not corrupt later behavior.", "function_signature": "std::vector<long long> processSessionOps(const std::vector<std::string>& ops);", "input_format": "Each string is one operation in one of these forms: \"OPEN account session token\", \"ALIAS token new_token\", \"REFRESH account old_session new_session new_token\", \"REVOKE session\", or \"QUERY token\". Operations are already ordered by time.", "output_format": "Return a vector containing the answer for every QUERY operation, in order.", "constraints": ["1 <= ops.size() <= 200000", "All ids are signed 64-bit integers encoded in decimal", "A token may be aliased multiple times over its lifetime"], "solver_prompt": "Solve this problem in cpp.\n\nA backend service receives a chronological stream of operations that create user sessions, refresh them, attach aliases, revoke them, and query the currently valid owner for a token. A session may be replaced by a newer session for the same account, aliases may be reassigned, and revocation of a session must also invalidate every token currently bound to it. Implement the processor so that each query returns the visible account id at that time or -1 if the token is no longer valid. Tokens and aliases can appear in operations before they are ever created, and repeated revocations or refreshes must not corrupt later behavior.\n\nFunction signature:\nstd::vector<long long> processSessionOps(const std::vector<std::string>& ops);\n\nInput:\nEach string is one operation in one of these forms: \"OPEN account session token\", \"ALIAS token new_token\", \"REFRESH account old_session new_session new_token\", \"REVOKE session\", or \"QUERY token\". Operations are already ordered by time.\n\nOutput:\nReturn a vector containing the answer for every QUERY operation, in order.\n\nConstraints:\n- 1 <= ops.size() <= 200000\n- All ids are signed 64-bit integers encoded in decimal\n- A token may be aliased multiple times over its lifetime\n\nReturn only code."}
{"id": "S00010", "language": "cpp", "difficulty": "hard", "problem_statement": "Researchers digitize a large corpus of inscriptions where each record is a token sequence over a custom alphabet, along with uncertain character substitutions and region-specific rewrite rules. Two inscriptions are considered equivalent if one can be transformed into the other using any number of allowed substitutions and rewrites while preserving token boundary markers. Build the requested templated function that groups record ids into equivalence classes and returns them in a deterministic order. The test harness instantiates the template with different integer id types, so the exact template header and return nesting are part of the required callable interface.", "function_signature": "template <typename IdT> std::vector<std::vector<IdT>> group_equivalent_inscriptions(const std::vector<Record<IdT>>& records, const std::vector<Rule>& rules);", "input_format": "records is a vector of Record<IdT> objects, each containing an id of type IdT and a token sequence. rules is a vector of Rule objects describing substitutions and rewrites over token strings.", "output_format": "Return a vector of groups. Inside each group, ids are sorted increasingly. The outer vector is sorted by the smallest id in each group. Only groups of size at least 2 should be included.", "constraints": ["1 <= records.size() <= 150000", "1 <= rules.size() <= 200000", "Total token count across all records <= 1000000", "Template parameter IdT supports copying and strict weak ordering"], "solver_prompt": "Solve this problem in cpp.\n\nResearchers digitize a large corpus of inscriptions where each record is a token sequence over a custom alphabet, along with uncertain character substitutions and region-specific rewrite rules. Two inscriptions are considered equivalent if one can be transformed into the other using any number of allowed substitutions and rewrites while preserving token boundary markers. Build the requested templated function that groups record ids into equivalence classes and returns them in a deterministic order. The test harness instantiates the template with different integer id types, so the exact template header and return nesting are part of the required callable interface.\n\nFunction signature:\ntemplate <typename IdT> std::vector<std::vector<IdT>> group_equivalent_inscriptions(const std::vector<Record<IdT>>& records, const std::vector<Rule>& rules);\n\nInput:\nrecords is a vector of Record<IdT> objects, each containing an id of type IdT and a token sequence. rules is a vector of Rule objects describing substitutions and rewrites over token strings.\n\nOutput:\nReturn a vector of groups. Inside each group, ids are sorted increasingly. The outer vector is sorted by the smallest id in each group. Only groups of size at least 2 should be included.\n\nConstraints:\n- 1 <= records.size() <= 150000\n- 1 <= rules.size() <= 200000\n- Total token count across all records <= 1000000\n- Template parameter IdT supports copying and strict weak ordering\n\nReturn only code."}
{"id": "S70010", "language": "cpp", "difficulty": "hard", "problem_statement": "A factory workcell stores recipes that can be drafted, activated into a machine slot, superseded by a newer recipe revision, rolled back to a named snapshot, deleted, and queried for the currently active parameter sum in a slot. Draft revisions are mutable until activation. Activation freezes that revision into the slot and any later superseding operation replaces the live active revision for that slot while retiring the previous active revision id. Rollback can only target snapshots created from activated revisions and produces a fresh active revision id. Implement the event processor so that each query reports the parameter sum in the slot or -1 if the slot has no active recipe.", "function_signature": "std::vector<long long> processRecipeHistory(const std::vector<std::string>& events);", "input_format": "Each event string is one of: \"DRAFT rev sum\", \"EDIT rev delta\", \"ACTIVATE rev slot\", \"SNAPSHOT rev snap\", \"SUPERSEDE old_rev new_rev new_sum\", \"ROLLBACK snap new_rev slot\", \"DELETE rev\", or \"QUERY slot\".", "output_format": "Return the parameter sum for every QUERY in order.", "constraints": ["1 <= events.size() <= 160000", "Revision ids, snapshot ids, and slot ids are integers", "Parameter sums and deltas fit in signed 64-bit integers"], "solver_prompt": "Solve this problem in cpp.\n\nA factory workcell stores recipes that can be drafted, activated into a machine slot, superseded by a newer recipe revision, rolled back to a named snapshot, deleted, and queried for the currently active parameter sum in a slot. Draft revisions are mutable until activation. Activation freezes that revision into the slot and any later superseding operation replaces the live active revision for that slot while retiring the previous active revision id. Rollback can only target snapshots created from activated revisions and produces a fresh active revision id. Implement the event processor so that each query reports the parameter sum in the slot or -1 if the slot has no active recipe.\n\nFunction signature:\nstd::vector<long long> processRecipeHistory(const std::vector<std::string>& events);\n\nInput:\nEach event string is one of: \"DRAFT rev sum\", \"EDIT rev delta\", \"ACTIVATE rev slot\", \"SNAPSHOT rev snap\", \"SUPERSEDE old_rev new_rev new_sum\", \"ROLLBACK snap new_rev slot\", \"DELETE rev\", or \"QUERY slot\".\n\nOutput:\nReturn the parameter sum for every QUERY in order.\n\nConstraints:\n- 1 <= events.size() <= 160000\n- Revision ids, snapshot ids, and slot ids are integers\n- Parameter sums and deltas fit in signed 64-bit integers\n\nReturn only code."}
{"id": "S70002", "language": "cpp", "difficulty": "hard", "problem_statement": "A fleet controller manages robots and detachable battery packs. Logs describe robots acquiring packs, dropping packs, packs being marked faulty, packs getting repaired with a new serial, and inspections asking which robot currently carries usable energy from a given serial lineage. When a pack is repaired, the previous serial becomes retired and any future event that references it should behave according to the last known state of that retired object lineage. Implement the inspection engine so that it produces the current robot id for each inspection or 0 if no robot is validly carrying a usable pack from that lineage at that time.", "function_signature": "std::vector<int> inspectBatteryAssignments(const std::vector<std::array<int, 4>>& events);", "input_format": "Each event is encoded as four integers. event[0] is the type: 1=attach(robot, pack, 0), 2=detach(robot, pack, 0), 3=fault(pack, 0, 0), 4=repair(old_pack, new_pack, 0), 5=query(pack, 0, 0). Remaining positions store the needed ids for that type.", "output_format": "Return one integer per query event, in order.", "constraints": ["1 <= events.size() <= 300000", "1 <= robot ids, pack ids <= 1000000000", "A robot can hold at most one active pack at a time"], "solver_prompt": "Solve this problem in cpp.\n\nA fleet controller manages robots and detachable battery packs. Logs describe robots acquiring packs, dropping packs, packs being marked faulty, packs getting repaired with a new serial, and inspections asking which robot currently carries usable energy from a given serial lineage. When a pack is repaired, the previous serial becomes retired and any future event that references it should behave according to the last known state of that retired object lineage. Implement the inspection engine so that it produces the current robot id for each inspection or 0 if no robot is validly carrying a usable pack from that lineage at that time.\n\nFunction signature:\nstd::vector<int> inspectBatteryAssignments(const std::vector<std::array<int, 4>>& events);\n\nInput:\nEach event is encoded as four integers. event[0] is the type: 1=attach(robot, pack, 0), 2=detach(robot, pack, 0), 3=fault(pack, 0, 0), 4=repair(old_pack, new_pack, 0), 5=query(pack, 0, 0). Remaining positions store the needed ids for that type.\n\nOutput:\nReturn one integer per query event, in order.\n\nConstraints:\n- 1 <= events.size() <= 300000\n- 1 <= robot ids, pack ids <= 1000000000\n- A robot can hold at most one active pack at a time\n\nReturn only code."}
{"id": "S00021", "language": "cpp", "difficulty": "hard", "problem_statement": "An airport receives a chronologically ordered stream of pallet transfer records. Each record contains a flight code, a pallet identifier, a destination zone, a weight, and a timestamp. Multiple records may refer to the same pallet as it is moved between holding zones before final loading. Build a component that ingests the records and produces, for every flight, the final sequence of pallets that should remain assigned after canceling moves that are superseded by later records for the same pallet. The output must preserve the first time each surviving pallet became assigned to its final flight, while total weight per destination zone is also reported for that flight.", "function_signature": "class PalletRebatcher { public: void ingest(const std::vector<std::string>& records); std::vector<std::string> flights() const; std::vector<std::string> finalManifest(const std::string& flightCode) const; std::map<std::string, long long> zoneWeights(const std::string& flightCode) const; };", "input_format": "The ingest method receives records encoded as strings in the form \"timestamp flight pallet zone weight action\", where action is either ASSIGN or REMOVE. Timestamps are nondecreasing across the vector.", "output_format": "flights returns all flight codes with at least one surviving pallet in lexicographic order. finalManifest returns pallet identifiers in stable order for the given flight. zoneWeights returns the summed surviving weight per zone for that flight.", "constraints": ["1 <= total number of records <= 3e5", "flight, pallet, and zone codes use printable ASCII without spaces", "1 <= weight <= 1e6"], "solver_prompt": "Solve this problem in cpp.\n\nAn airport receives a chronologically ordered stream of pallet transfer records. Each record contains a flight code, a pallet identifier, a destination zone, a weight, and a timestamp. Multiple records may refer to the same pallet as it is moved between holding zones before final loading. Build a component that ingests the records and produces, for every flight, the final sequence of pallets that should remain assigned after canceling moves that are superseded by later records for the same pallet. The output must preserve the first time each surviving pallet became assigned to its final flight, while total weight per destination zone is also reported for that flight.\n\nFunction signature:\nclass PalletRebatcher { public: void ingest(const std::vector<std::string>& records); std::vector<std::string> flights() const; std::vector<std::string> finalManifest(const std::string& flightCode) const; std::map<std::string, long long> zoneWeights(const std::string& flightCode) const; };\n\nInput:\nThe ingest method receives records encoded as strings in the form \"timestamp flight pallet zone weight action\", where action is either ASSIGN or REMOVE. Timestamps are nondecreasing across the vector.\n\nOutput:\nflights returns all flight codes with at least one surviving pallet in lexicographic order. finalManifest returns pallet identifiers in stable order for the given flight. zoneWeights returns the summed surviving weight per zone for that flight.\n\nConstraints:\n- 1 <= total number of records <= 3e5\n- flight, pallet, and zone codes use printable ASCII without spaces\n- 1 <= weight <= 1e6\n\nReturn only code."}
{"id": "S01025", "language": "python", "difficulty": "hard", "problem_statement": "A mapping service stores rectangular paint operations on an infinite integer grid as `(x1, y1, x2, y2, terrain)` covering all cells with `x1 <= x < x2` and `y1 <= y < y2`. Later operations overwrite earlier ones. After all operations, report one digest row per non-empty terrain. Each row must contain the terrain name, total painted cell count, and the lexicographically smallest covered cell `(x, y)`. Rows are ordered by quadrant of that smallest cell in the sequence `NE`, `NW`, `SW`, `SE`, then by terrain name ascending, where cells on axes belong to the quadrant reached by treating zero as non-negative. The grid is sparse and coordinates can be large.", "function_signature": "def tile_coverage_digest(paints: list[tuple[int, int, int, int, str]]) -> list[tuple[str, int, tuple[int, int]]]:", "input_format": "A list of 5-tuples `(x1, y1, x2, y2, terrain)` describing half-open rectangles in overwrite order.", "output_format": "Return a list of tuples `(terrain, covered_cells, (min_x, min_y))` sorted by the required quadrant order and then terrain name.", "constraints": ["1 <= len(paints) <= 500000", "-10^9 <= x1 < x2 <= 10^9", "-10^9 <= y1 < y2 <= 10^9", "terrain is a non-empty ASCII string"], "solver_prompt": "Solve this problem in python.\n\nA mapping service stores rectangular paint operations on an infinite integer grid as `(x1, y1, x2, y2, terrain)` covering all cells with `x1 <= x < x2` and `y1 <= y < y2`. Later operations overwrite earlier ones. After all operations, report one digest row per non-empty terrain. Each row must contain the terrain name, total painted cell count, and the lexicographically smallest covered cell `(x, y)`. Rows are ordered by quadrant of that smallest cell in the sequence `NE`, `NW`, `SW`, `SE`, then by terrain name ascending, where cells on axes belong to the quadrant reached by treating zero as non-negative. The grid is sparse and coordinates can be large.\n\nFunction signature:\ndef tile_coverage_digest(paints: list[tuple[int, int, int, int, str]]) -> list[tuple[str, int, tuple[int, int]]]:\n\nInput:\nA list of 5-tuples `(x1, y1, x2, y2, terrain)` describing half-open rectangles in overwrite order.\n\nOutput:\nReturn a list of tuples `(terrain, covered_cells, (min_x, min_y))` sorted by the required quadrant order and then terrain name.\n\nConstraints:\n- 1 <= len(paints) <= 500000\n- -10^9 <= x1 < x2 <= 10^9\n- -10^9 <= y1 < y2 <= 10^9\n- terrain is a non-empty ASCII string\n\nReturn only code."}
{"id": "S00015", "language": "cpp", "difficulty": "hard", "problem_statement": "A localization pipeline must align subtitle segments from two languages. Each segment has a start time, end time, and text. Two segments can be paired if their overlap ratio is at least t, where overlap ratio is intersection duration divided by union duration. Find a maximum-cardinality pairing with no segment used more than once, and among all such pairings minimize total absolute difference in text lengths. Return the chosen index pairs in ascending order by the left-language index. The interface exposes two overloads, one for string texts and one for precomputed lengths.", "function_signature": "class SubtitleAligner { public: std::vector<std::pair<int,int>> align(const std::vector<std::tuple<int,int,std::string>>& a, const std::vector<std::tuple<int,int,std::string>>& b, double t) const; std::vector<std::pair<int,int>> align(const std::vector<std::tuple<int,int,int>>& a, const std::vector<std::tuple<int,int,int>>& b, double t) const; };", "input_format": "For the string overload, each tuple is {start_ms, end_ms, text}. For the length overload, each tuple is {start_ms, end_ms, text_length}. Times are integers and segments in each language are sorted by start time.", "output_format": "Return selected pairs {i, j} with zero-based indices into the left and right arrays. The result must be sorted by i ascending.", "constraints": ["1 <= a.size(), b.size() <= 50000", "Total characters across both string inputs <= 800000", "0 <= start_ms < end_ms <= 1000000000", "0.0 <= t <= 1.0"], "solver_prompt": "Solve this problem in cpp.\n\nA localization pipeline must align subtitle segments from two languages. Each segment has a start time, end time, and text. Two segments can be paired if their overlap ratio is at least t, where overlap ratio is intersection duration divided by union duration. Find a maximum-cardinality pairing with no segment used more than once, and among all such pairings minimize total absolute difference in text lengths. Return the chosen index pairs in ascending order by the left-language index. The interface exposes two overloads, one for string texts and one for precomputed lengths.\n\nFunction signature:\nclass SubtitleAligner { public: std::vector<std::pair<int,int>> align(const std::vector<std::tuple<int,int,std::string>>& a, const std::vector<std::tuple<int,int,std::string>>& b, double t) const; std::vector<std::pair<int,int>> align(const std::vector<std::tuple<int,int,int>>& a, const std::vector<std::tuple<int,int,int>>& b, double t) const; };\n\nInput:\nFor the string overload, each tuple is {start_ms, end_ms, text}. For the length overload, each tuple is {start_ms, end_ms, text_length}. Times are integers and segments in each language are sorted by start time.\n\nOutput:\nReturn selected pairs {i, j} with zero-based indices into the left and right arrays. The result must be sorted by i ascending.\n\nConstraints:\n- 1 <= a.size(), b.size() <= 50000\n- Total characters across both string inputs <= 800000\n- 0 <= start_ms < end_ms <= 1000000000\n- 0.0 <= t <= 1.0\n\nReturn only code."}
{"id": "S00012", "language": "cpp", "difficulty": "hard", "problem_statement": "A molecular docking pipeline stores atom centers for a receptor and then asks many query points representing candidate ligand atom positions. For each query, determine whether it lies within radius r of any receptor atom, and also return the index of the nearest receptor atom with ties broken by smaller index. Distances are Euclidean in three dimensions, coordinates are integers, and the number of queries is large enough that a naive scan per query will time out. The required interface exposes a reusable object built from receptor atoms and then queried repeatedly.", "function_signature": "class ClashIndex { public: ClashIndex(const std::vector<std::array<int,3>>& atoms, int r); std::pair<bool,int> operator()(const std::array<int,3>& point) const; };", "input_format": "The constructor receives receptor atom coordinates and a nonnegative integer radius r. Each call to operator() receives one query point {x, y, z}.", "output_format": "For a query point, return {has_clash, nearest_atom_index}. If atoms is empty, return {false, -1}. Indices are zero-based in the original atom order.", "constraints": ["0 <= atoms.size() <= 20000", "1 <= number of queries <= 100000", "|coordinate| <= 1000000", "0 <= r <= 1000000"], "solver_prompt": "Solve this problem in cpp.\n\nA molecular docking pipeline stores atom centers for a receptor and then asks many query points representing candidate ligand atom positions. For each query, determine whether it lies within radius r of any receptor atom, and also return the index of the nearest receptor atom with ties broken by smaller index. Distances are Euclidean in three dimensions, coordinates are integers, and the number of queries is large enough that a naive scan per query will time out. The required interface exposes a reusable object built from receptor atoms and then queried repeatedly.\n\nFunction signature:\nclass ClashIndex { public: ClashIndex(const std::vector<std::array<int,3>>& atoms, int r); std::pair<bool,int> operator()(const std::array<int,3>& point) const; };\n\nInput:\nThe constructor receives receptor atom coordinates and a nonnegative integer radius r. Each call to operator() receives one query point {x, y, z}.\n\nOutput:\nFor a query point, return {has_clash, nearest_atom_index}. If atoms is empty, return {false, -1}. Indices are zero-based in the original atom order.\n\nConstraints:\n- 0 <= atoms.size() <= 20000\n- 1 <= number of queries <= 100000\n- |coordinate| <= 1000000\n- 0 <= r <= 1000000\n\nReturn only code."}
{"id": "S01068", "language": "python", "difficulty": "hard", "problem_statement": "A tournament backend stores draft actions for many matches. Each action gives a match id, team, map name, step number, and verb BAN, PICK, UNBAN, or REPICK. Reconstruct the final transcript by processing actions in increasing step number, and for equal step numbers by later input order. A BAN removes a map from availability, UNBAN restores it only if it was previously banned, PICK records the map if still available, and REPICK replaces the most recent recorded pick by the same team in that match. Export one line per recorded pick in final transcript order, grouped by match id. Every match seen in input but ending with no recorded picks must still emit a single transcript line using a mandated placeholder team and map token.", "function_signature": "def build_veto_transcript(actions: list[dict]) -> list[str]:", "input_format": "actions is a list of dictionaries with keys: match_id (str), team (str), map (str), step (int), verb (str).", "output_format": "Return lines formatted as \"<match_id>|<index>|<team>|<map>\" where index is 1-based within the final transcript for that match. Empty matches return \"<match_id>|0|NONE|NO_MAP\".", "constraints": ["1 <= len(actions) <= 140000", "0 <= step <= 10^9", "verb is one of BAN, PICK, UNBAN, REPICK"], "solver_prompt": "Solve this problem in python.\n\nA tournament backend stores draft actions for many matches. Each action gives a match id, team, map name, step number, and verb BAN, PICK, UNBAN, or REPICK. Reconstruct the final transcript by processing actions in increasing step number, and for equal step numbers by later input order. A BAN removes a map from availability, UNBAN restores it only if it was previously banned, PICK records the map if still available, and REPICK replaces the most recent recorded pick by the same team in that match. Export one line per recorded pick in final transcript order, grouped by match id. Every match seen in input but ending with no recorded picks must still emit a single transcript line using a mandated placeholder team and map token.\n\nFunction signature:\ndef build_veto_transcript(actions: list[dict]) -> list[str]:\n\nInput:\nactions is a list of dictionaries with keys: match_id (str), team (str), map (str), step (int), verb (str).\n\nOutput:\nReturn lines formatted as \"<match_id>|<index>|<team>|<map>\" where index is 1-based within the final transcript for that match. Empty matches return \"<match_id>|0|NONE|NO_MAP\".\n\nConstraints:\n- 1 <= len(actions) <= 140000\n- 0 <= step <= 10^9\n- verb is one of BAN, PICK, UNBAN, REPICK\n\nReturn only code."}
{"id": "S01012", "language": "python", "difficulty": "hard", "problem_statement": "A fleet of cache nodes emits invalidation events. Each event has a region, a node id, a cache key, a version number, and an action of either SET or DROP. For each region and key, process events in input order to determine the final live version per node. A DROP removes the key from that node only if the dropped version exactly matches the current live version there. After processing all events, produce one digest row per region and key where at least one node still has a live version. Each row must contain the region, key, the number of live nodes, a slash-joined node summary ordered by version descending then node id ascending, and a quorum field that is the literal string \"YES\" if at least 3 live nodes share the same version, otherwise \"NO\".", "function_signature": "def build_invalidation_digest(events: list[dict]) -> list[dict]:", "input_format": "A list of dictionaries with keys: region (str), node (str), key (str), version (int), action (str equal to 'SET' or 'DROP').", "output_format": "Return a list of dictionaries with keys: region, key, live_nodes, summary, quorum. Sort rows by region ascending then key ascending.", "constraints": ["1 <= len(events) <= 200000", "1 <= version <= 10**9", "region, node, and key are printable strings of length at most 24"], "solver_prompt": "Solve this problem in python.\n\nA fleet of cache nodes emits invalidation events. Each event has a region, a node id, a cache key, a version number, and an action of either SET or DROP. For each region and key, process events in input order to determine the final live version per node. A DROP removes the key from that node only if the dropped version exactly matches the current live version there. After processing all events, produce one digest row per region and key where at least one node still has a live version. Each row must contain the region, key, the number of live nodes, a slash-joined node summary ordered by version descending then node id ascending, and a quorum field that is the literal string \"YES\" if at least 3 live nodes share the same version, otherwise \"NO\".\n\nFunction signature:\ndef build_invalidation_digest(events: list[dict]) -> list[dict]:\n\nInput:\nA list of dictionaries with keys: region (str), node (str), key (str), version (int), action (str equal to 'SET' or 'DROP').\n\nOutput:\nReturn a list of dictionaries with keys: region, key, live_nodes, summary, quorum. Sort rows by region ascending then key ascending.\n\nConstraints:\n- 1 <= len(events) <= 200000\n- 1 <= version <= 10**9\n- region, node, and key are printable strings of length at most 24\n\nReturn only code."}
{"id": "S00003", "language": "html", "difficulty": "hard", "problem_statement": "Build a semantic HTML document for a digital legal filing package that contains a case caption, a table of contents, the main motion, several numbered argument sections with nested subarguments, an exhibit index, and appended exhibits. Each exhibit includes a title, reference number, and one or more descriptive paragraphs. The arguments section contains quoted authority, enumerated factors, and a final requested relief section. The filing must keep the main pleading distinct from appended materials, maintain a strict heading hierarchy across arguments and subarguments, and represent the exhibit index in a form that matches its role as a navigational listing rather than body prose. The page should be suitable for reading linearly and for jumping between major filing components.", "function_signature": "filing_package.html", "input_format": "No runtime input. Create one static HTML filing page following the described document structure.", "output_format": "A valid HTML document with clear document regions, nested argument sections, a contents listing, an exhibit index, and appended exhibit sections.", "constraints": ["The motion body and exhibits must be separate structural regions", "Nested arguments must use descending heading levels in order", "The table of contents and exhibit index must be represented as navigational listings", "Quoted authority and enumerated factors must preserve their semantic distinction"], "solver_prompt": "Solve this problem in html.\n\nBuild a semantic HTML document for a digital legal filing package that contains a case caption, a table of contents, the main motion, several numbered argument sections with nested subarguments, an exhibit index, and appended exhibits. Each exhibit includes a title, reference number, and one or more descriptive paragraphs. The arguments section contains quoted authority, enumerated factors, and a final requested relief section. The filing must keep the main pleading distinct from appended materials, maintain a strict heading hierarchy across arguments and subarguments, and represent the exhibit index in a form that matches its role as a navigational listing rather than body prose. The page should be suitable for reading linearly and for jumping between major filing components.\n\nRequired output form:\nfiling_package.html\n\nInput:\nNo runtime input. Create one static HTML filing page following the described document structure.\n\nOutput:\nA valid HTML document with clear document regions, nested argument sections, a contents listing, an exhibit index, and appended exhibit sections.\n\nConstraints:\n- The motion body and exhibits must be separate structural regions\n- Nested arguments must use descending heading levels in order\n- The table of contents and exhibit index must be represented as navigational listings\n- Quoted authority and enumerated factors must preserve their semantic distinction\n\nReturn only code."}
{"id": "S00013", "language": "python", "difficulty": "hard", "problem_statement": "A stadium represents each seat as occupied or empty. Between queries, seats may flip state. For each group size k, return the minimum number of contiguous empty seat blocks needed to place exactly k spectators, allowing a block to be partially used at its ends. If fewer than k empty seats exist in total, return -1. Empty seats scattered across many runs are not interchangeable with one long run when minimizing the number of blocks.", "function_signature": "def min_blocks_for_groups(n: int, operations: list[tuple[str, int, int]]) -> list[int]:", "input_format": "The stadium has seats 1..n, initially all empty unless specified by update operations. operations contains either ('flip', l, r) to toggle occupancy in a range or ('ask', k, 0) to query the current layout.", "output_format": "For each ask operation, output the minimum number of contiguous empty blocks needed to seat exactly k spectators, or -1 if impossible.", "constraints": ["1 <= n <= 3 * 10^5", "1 <= len(operations) <= 3 * 10^5", "1 <= k <= n", "1 <= l <= r <= n"], "solver_prompt": "Solve this problem in python.\n\nA stadium represents each seat as occupied or empty. Between queries, seats may flip state. For each group size k, return the minimum number of contiguous empty seat blocks needed to place exactly k spectators, allowing a block to be partially used at its ends. If fewer than k empty seats exist in total, return -1. Empty seats scattered across many runs are not interchangeable with one long run when minimizing the number of blocks.\n\nFunction signature:\ndef min_blocks_for_groups(n: int, operations: list[tuple[str, int, int]]) -> list[int]:\n\nInput:\nThe stadium has seats 1..n, initially all empty unless specified by update operations. operations contains either ('flip', l, r) to toggle occupancy in a range or ('ask', k, 0) to query the current layout.\n\nOutput:\nFor each ask operation, output the minimum number of contiguous empty blocks needed to seat exactly k spectators, or -1 if impossible.\n\nConstraints:\n- 1 <= n <= 3 * 10^5\n- 1 <= len(operations) <= 3 * 10^5\n- 1 <= k <= n\n- 1 <= l <= r <= n\n\nReturn only code."}
